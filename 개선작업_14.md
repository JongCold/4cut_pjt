# 개선작업 14: 다운로드 즉시 파기 프로세스 개편, 테두리 색상 동기화, 동영상 404 방지 및 Vercel 실시간 배포

**문서 번호**: 4CUTS-IMPROVE-014  
**작업 일시**: 2026-09-13  
**작업 요약**:
1. **다운로드 즉시 파기 & 24시간 보존 2중 안전망 구축**: 고객이 QR 코드로 접속하여 사진/영상을 다운로드받는 즉시 구글 드라이브와 로컬 서버에서 영구 파기하고, 다운로드받지 않은 경우 24시간 보존 후 자동 파기되도록 프로세스 전면 개편.
2. **동영상 404 Not Found 에러 원천 해결**: 사진 다운로드 시 세션 전체 파일이 삭제되어 동영상이 지워지던 문제를 미디어별 정밀 분리 파기(`destroy_type: photo / video / all`)로 해결.
3. **인화 테두리 색상 동기화**: 태블릿에서 프레임 색상(크림, 블랙, 레드, 네이비 등) 변경 시 기본 프레임 파일 덮어쓰기, 구글 드라이브 재업로드 및 새 색상이 반영된 QR 코드를 실시간 갱신하여 모바일 다운로드에서도 선택 색상 100% 반영.
4. **모바일 뷰어 상단 모서리 삐져나옴 UI 수정**: `.mobile-viewer-card`의 32px 둥근 모서리 바깥으로 헤더 사각형 배경이 튀어나오던 문제를 `overflow: hidden;` 및 상단 반경 보정으로 완벽 클리핑.
5. **불필요한 일괄 다운로드 UX 제거**: 고객이 결과물을 확인하고 내려받는 자연스러운 동선에 맞추어 `사진+영상 한 번에 저장` 버튼을 제거하고 개별 탭 확인 저장 UX 복원.
6. **Vercel 실시간 배포 완료**: 최신 프론트엔드 및 백엔드 코드를 GitHub `main` 브랜치에 커밋 및 푸시하여 `https://4cut-pjt.vercel.app/download.html` 라이브 도메인에 최신 디자인 배포 완료.

---

## 1. 주요 문제 분석 및 해결 내역

| 문제 현상 | 근본 원인 | 해결 조치 |
| :--- | :--- | :--- |
| **다운로드 후에도 최대 24시간 동안 클라우드/서버에 개인정보 잔존** | 기존에는 APScheduler 및 GAS 24시간 만료 스케줄러에만 의존하여, 고객이 다운로드를 마친 후에도 파일이 즉시 파기되지 않음. | 모바일 다운로드 버튼 클릭 즉시 구글 드라이브(`GAS Webhook`)와 로컬 서버(`/api/cleanup-session`)로 파기 명령을 전송하여 즉각 영구 삭제. 미다운로드 파일만 24시간 유지 후 자동 삭제. |
| **동영상 다운로드 시 404 Not Found 발생** (`behind_video_*.mp4`) | 사진 다운로드 버튼 클릭 시 즉시 파기 로직이 세션 ID 전체를 기준으로 디렉토리 내 파일을 삭제하면서 아직 받지 않은 동영상까지 함께 삭제해 버림. | 파기 함수에 `destroy_type` 필터를 추가하여 사진 다운로드 시에는 오직 사진만 삭제하고 동영상은 보존, 동영상 다운로드 시에만 동영상을 삭제하도록 분리. |
| **태블릿에서 테두리 색상을 변경해도 모바일 다운로드 시 하얀색으로 노출** | `/api/update-frame-color` 호출 시 새 색상의 파일은 생성되었으나, 구글 드라이브에 업로드되지 않고 QR 코드 URL 및 이미지가 갱신되지 않아 최초의 하얀색 프레임을 계속 참조함. | 색상 변경 시 기본 프레임 파일(`ai_frame_{sid}.jpg`)을 즉시 덮어쓰고, Google Drive에 새 프레임을 업로드하며, 새 색상이 적용된 QR 코드를 실시간 재생성하여 화면의 QR 이미지를 즉각 갱신. |
| **상단 모서리 헤더 사각형이 둥근 카드 테두리 밖으로 삐져나옴** | 카드 컨테이너에 `border-radius: 32px`가 있었으나 `overflow: hidden;`이 누락되어 직사각형 헤더 배경이 모서리 밖으로 돌출됨. | `.mobile-viewer-card`에 `overflow: hidden;` 및 `.app-header` 상단 모서리에 `border-top-left-radius: 29px; border-top-right-radius: 29px;` 적용. |
| **모바일 접속 시 이전 구형 디자인(보라색) 노출** | 로컬 파일만 수정되고 GitHub에 git push가 되지 않아 Vercel에 구버전 정적 파일이 배포되어 있었음. | GitHub `main` 브랜치로 커밋 및 푸시 완료하여 Vercel 실시간 자동 빌드/배포 완료 (`https://4cut-pjt.vercel.app/download.html`). |

---

## 2. 수정된 파일 내역

1. **`local-server/google_drive_gas.js`**:
   - `action === "delete_files"` 신규 핸들러 추가 (구글 드라이브 특정 파일 ID 즉시 휴지통 이동/파기).
   - 기존 24시간 만료 자동 파기 함수 `cleanupOldFiles()` 유지 (미다운로드 대비).
2. **`local-server/app.py`**:
   - `destroy_google_drive_files(file_ids)`: GAS Webhook 및 Service Account API 이중 파기 호출 함수 구현.
   - `destroy_session_files_immediately(session_id, ..., destroy_type)`: 미디어 유형별 정밀 파기 구현 (사진 다운로드 시 동영상 삭제 방지).
   - `api_cleanup_session`: 즉시 파기 및 `destroy_type` 파라미터 처리.
   - `api_update_frame_color`: 프레임 색상 변경 시 기본 프레임 덮어쓰기, 구글 드라이브 업로드, 새 QR 코드 실시간 생성 및 반환.
3. **`local-server/templates/index.html`**:
   - 프레임 색상 변경 응답 수신 시 키오스크 화면의 QR 코드(`qr-code-img`) 실시간 갱신.
   - QR 다운로드 카드 안내 뱃지: `다운로드 즉시 파기 (미다운로드 시 24h)`.
4. **`vercel-frontend/download.html`**:
   - 상단 모서리 삐져나옴 클리핑 수정 (`overflow: hidden;`).
   - 불필요한 일괄 다운로드 버튼 제거 및 개별 탭 확인 UX 유지.
   - 다운로드 즉시 파기 통신 로직(`triggerInstantDestruction`) 탑재 (로컬 백엔드 및 외부 LTE/5G 대비 GAS Webhook 다이렉트 호출).
   - 실시간 파기 완료 피드백 배너 및 버튼 상태 전환.
5. **`vercel-frontend/index.html`**:
   - 개인정보 안심 파기 안내 문구 동기화.

---

## 3. 핵심 구현 소스코드

### 3.1. Google Apps Script 특정 파일 즉시 파기 (`google_drive_gas.js`)
```javascript
// doPost 내 고객 다운로드 즉시 파기 명령 수신 처리
if (data.action === "delete_files") {
  const fileIds = data.fileIds || [];
  const results = [];
  let successCount = 0;
  fileIds.forEach(function(fid) {
    if (fid) {
      try {
        const file = DriveApp.getFileById(fid);
        file.setTrashed(true); // 휴지통으로 이동하여 즉시 접근 차단
        results.push({ fileId: fid, status: "trashed" });
        successCount++;
      } catch (err) {
        results.push({ fileId: fid, status: "error", message: err.toString() });
      }
    }
  });
  Logger.log("[Immediate Destroy] Trashed " + successCount + " files: " + JSON.stringify(fileIds));
  return ContentService.createTextOutput(JSON.stringify({
    status: "success",
    message: "Immediate destruction completed",
    successCount: successCount,
    results: results
  })).setMimeType(ContentService.MimeType.JSON);
}
```

### 3.2. 백엔드 미디어별 정밀 파기 로직 (`app.py`)
```python
def destroy_session_files_immediately(session_id: str, gdrive_ids: Optional[List[str]] = None, target_files: Optional[List[str]] = None, destroy_type: str = "all"):
    """
    고객 모바일 다운로드 확인 즉시 구글 드라이브 및 로컬 세션 파일 영구 파기
    - destroy_type == 'photo': 사진 관련 파일만 파기 (동영상 보존)
    - destroy_type == 'video': 동영상 관련 파일만 파기 (사진 보존)
    - destroy_type == 'all': 해당 세션의 모든 미디어 파기
    """
    if gdrive_ids:
        destroy_google_drive_files(gdrive_ids)

    local_deleted = 0
    try:
        if os.path.exists(UPLOAD_DIR):
            for fname in os.listdir(UPLOAD_DIR):
                is_session_match = bool(session_id and session_id in fname)
                is_target_match = bool(target_files and fname in target_files)
                
                if not (is_session_match or is_target_match):
                    continue
                    
                is_video_file = ("behind_video" in fname) or fname.endswith(".mp4") or fname.endswith(".webm")
                
                should_delete = False
                if destroy_type == "all":
                    should_delete = True
                elif destroy_type == "photo":
                    if not is_video_file: # 사진만 다운로드 시 동영상 절대 보존!
                        should_delete = True
                elif destroy_type == "video":
                    if is_video_file:
                        should_delete = True
                else:
                    should_delete = True
                    
                if should_delete:
                    fpath = os.path.join(UPLOAD_DIR, fname)
                    if os.path.isfile(fpath):
                        os.remove(fpath)
                        local_deleted += 1
    except Exception as ex:
        print(f"[Instant Destruction Error] 세션 정리 오류: {ex}")
```

### 3.3. 테두리 색상 변경 시 기본 프레임 덮어쓰기 & QR 실시간 갱신 (`app.py`)
```python
@app.post("/api/update-frame-color")
async def api_update_frame_color(req: FrameColorRequest, request: Request):
    # ... 프레임 렌더링 ...
    new_frame_name = f"{prefix}_frame_{session_id}_{color_safe}.jpg"
    new_frame_path = os.path.join(UPLOAD_DIR, new_frame_name)
    new_frame.save(new_frame_path, format="JPEG", quality=95)

    # 기본 파일명에도 즉시 덮어쓰기 (기존 URL 캐시 접속 시에도 변경 색상 보장)
    base_frame_name = f"{prefix}_frame_{session_id}.jpg"
    new_frame.save(os.path.join(UPLOAD_DIR, base_frame_name), format="JPEG", quality=95)

    # 구글 드라이브 업로드 & 새 QR 코드 생성
    new_img_drive_id = upload_to_google_drive(new_frame_path, new_frame_name, "image/jpeg", folder_id=GOOGLE_PHOTO_FOLDER_ID)
    
    # ... QR 코드 생성 ...
    return {
        "success": True,
        "frame_url": f"/uploads/{new_frame_name}",
        "postcard_url": f"/uploads/{new_postcard_name}",
        "frame_color": req.frame_color,
        "qr_code_base64": new_qr_base64,
        "download_url": download_url
    }
```

---

## 4. 검증 결과

1. **파기 로직 단위 테스트 (`test_destruction.py`)**:
   - 다운로드 즉시 파기 테스트: **PASS (100%)**
   - 24시간 보존 및 만료 파일 파기 테스트: **PASS (100%)**
2. **동영상 보존 검증**:
   - 사진 다운로드 시 `behind_video_*.mp4` 파일이 보존되어 `404 Not Found`가 발생하지 않음을 검증.
3. **Vercel 실시간 배포 검증**:
   - 브라우저 서브에이전트를 통해 라이브 도메인 [https://4cut-pjt.vercel.app/download.html](https://4cut-pjt.vercel.app/download.html) 접속.
   - 상단 모서리 완벽 클리핑, 최신 레트로 두들 탭 디자인, 불필요한 일괄 버튼 제거, 개인정보 안심 파기 안내 배너 렌더링 정상 확인 완료.
