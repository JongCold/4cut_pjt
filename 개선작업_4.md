# AI 4컷 포토부스 개선 작업 보고서 (개선작업_4.md)

## 1. 개요
본 보고서는 모바일(아이폰 iOS Safari) 환경에서 비하인드 동영상이 재생되지 않던 **미디어 엑박(손상 아이콘) 현상 해결** 및 구글 드라이브/로컬 파일 자체에 **실제 2배속 인코딩(H.264 MP4 변환) 적용** 내역을 요약합니다.

---

## 2. 주요 개선 및 오류 해결 내역

### 2.1. 아이폰(iOS Safari) 동영상 미재생(엑박) 원인 및 해결
- **문제 현상:**
  - 모바일(특히 아이폰)에서 QR 스캔 시 비하인드 동영상 영역에 재생 불가(손상 비디오) 회색 아이콘이 뜨며 재생 및 확인이 불가능했던 현상.
- **원인 분석:**
  - 웹캠에서 녹화된 원본 파일이 `.webm` 포맷으로 서버에 저장되었습니다. 아이폰 Safari 브라우저는 표준 `.webm` 동영상 컨테이너 재생을 기본 지원하지 않기 때문에 발생한 미디어 호환성 에러입니다.
- **조치 및 개선 사항:**
  - 백엔드 서버에 저장하고 구글 드라이브에 최종 업로드하는 파일을 **iOS/Android 표준 규격인 H.264 MP4 (`yuv420p` 픽셀 포맷)**로 재인코딩하여 저장하도록 변경했습니다.
  - `vercel-frontend/download.html` 뷰어의 비디오 재생 소스 마임 타입을 `video/mp4`로 수정하여 모든 기기에서 즉시 재생되도록 조치했습니다.

---

### 2.2. 구글 드라이브 및 로컬 비디오 파일 자체에 실제 2배속 인코딩 적용
- **문제 현상:**
  - 기존에는 모바일 플레이어 속도 조절(`playbackRate = 2.0`)만 처리하여, 파일을 다운로드하거나 구글 드라이브에서 직접 볼 때는 정속(1배속)으로 보이는 문제.
- **조치 및 개선 사항:**
  - 백엔드 `app.py`에 `imageio` 및 내장 `ffmpeg` 라이브러리를 연동하여, **동영상 파일 자체를 2배속으로 리인코딩하는 로직**을 추가했습니다.
  - 웹캠으로부터 수신된 원본 webm 파일의 프레임들을 읽어 들여 2프레임당 1프레임만 선택하여 합친 뒤, 2배속으로 빠르게 움직이는 고선명 MP4 파일로 직접 렌더링합니다.
  - 이를 통해 **로컬 저장소는 물론 구글 드라이브에 최종 보관되는 파일 자체도 리얼 2배속 가속 비디오**로 저장되도록 조치했습니다.

---

## 3. 최종 시스템 검증 및 변경 파일 요약

| 구분 | 파일 경로 | 변경 내용 |
| :--- | :--- | :--- |
| **백엔드 서버** | `local-server/app.py` | 2배속 MP4(H.264, yuv420p) 인코딩 헬퍼 탑재 및 변환 흐름 적용 |
| **모바일 뷰어** | `vercel-frontend/download.html` | 비하인드 비디오 재생 포맷 및 저장 확장자 `.mp4` 교체 |
| **개선 작업서** | `개선작업_4.md` | 현 4차 비디오 개선 내역 보고서 작성 |

---
**작성일:** 2026-08-25  
**작성자:** Antigravity AI Assistant
