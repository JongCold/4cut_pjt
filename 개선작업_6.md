# 🛠️ [개선작업 6차] 구글 드라이브 권한(403) 및 Quota 한계 에러 해결 & GAS 자동 파기 동기화 보고서

> **작업 일자**: 2026년 8월 26일  
> **관련 커밋**: `fix(storage): Fix GAS Video Folder ID, add cleanup action, and handle Service Account 403 quota/permissions gracefully`  
> **수정 파일**:
> - [`local-server/app.py`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/local-server/app.py)
> - [`local-server/google_drive_gas.js`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/local-server/google_drive_gas.js)
> - [`구글 드라이브 설정.md`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/%EA%B5%AC%EA%B8%80%20%EB%93%9C%EB%9D%BC%EC%9D%B4%EB%B8%8C%20%EC%84%A4%EC%A0%95.md)

---

## 1. 발생한 문제 현상 및 에러 로그

### 🚨 에러 1: 24시간 자동 파기 시 403 권한 부족 에러
```text
[Auto Cleanup Error] 구글 드라이브 파일 삭제 실패 (1qMgIYDiRuwqQNsKi99Li5T3YdtTVNdJh): 
<HttpError 403 when requesting https://www.googleapis.com/drive/v3/files/1qMgIYDiRuwqQNsKi99Li5T3YdtTVNdJh? 
returned "The user does not have sufficient permissions for this file.". 
Details: "[{'message': 'The user does not have sufficient permissions for this file.', 'domain': 'global', 'reason': 'insufficientFilePermissions'}]">
```

### 🚨 에러 2: 구글 드라이브 업로드 시 서비스 계정 0-Quota 제한 에러
```text
[Google Drive API] ⚠️ 업로드 실패 (Service Account Quota 정책 제한): 
<HttpError 403 when requesting https://www.googleapis.com/upload/drive/v3/files?fields=id%2C+webViewLink%2C+webContentLink&supportsAllDrives=true&alt=json&uploadType=resumable 
returned "Service Accounts do not have storage quota. Leverage shared drives or use OAuth delegation instead.". 
Details: "[{'message': 'Service Accounts do not have storage quota...', 'domain': 'usageLimits', 'reason': 'storageQuotaExceeded'}]">
```

---

## 2. 심층 원인 분석 (Root Cause Analysis)

### 📌 에러 1 원인 분석: 파일 소유권과 삭제 권한 불일치
1. 구글 드라이브에 저장된 사진/영상 파일들은 **Google Apps Script(GAS)를 통해 사용자 본인 계정의 소유권(Owner)**으로 생성됩니다.
2. 로컬 백엔드(`app.py`)의 `cleanup_expired_files()`는 `google-key.json`(서비스 계정)으로 구글 드라이브 API를 호출하여 파일 영구 삭제(`files().delete()`)를 시도했습니다.
3. 구글 드라이브 보안 정책상 일반 공유 폴더의 **편집자(Editor)** 권한을 가진 서비스 계정은 **다른 사용자(개인 계정)가 소유한 파일의 영구 삭제 권한이 없어 `insufficientFilePermissions` (403) 에러가 발생**한 것입니다.
4. 개인 계정 소유의 파일 삭제는 본인 계정 권한으로 동작하는 **GAS 내부의 `cleanupOldFiles()`**가 실행되어야 정상 처리됩니다.

### 📌 에러 2 원인 분석: GAS Webhook 실패 후 서비스 계정 Fallback 유입
1. `app.py`의 `upload_to_google_drive()`는 1순위로 `GAS_WEBHOOK_URL`로 업로드를 시도하고, 실패 시 2순위로 `DRIVE_SERVICE`(서비스 계정) 업로드를 시도합니다.
2. `google_drive_gas.js` 내의 `VIDEO_FOLDER_ID` 문자열 오타(`8YfnaQ` vs `8NfnaQ`) 또는 GAS 새 버전 미배포/권한 이슈로 인해 GAS 업로드가 실패하고 서비스 계정으로 Fallback되었습니다.
3. 서비스 계정은 구글 정책상 자체 스토리지 할당량이 0 Byte이므로, Fallback 시도 시 항상 `storageQuotaExceeded` 403 에러가 반환된 것입니다.

---

## 3. 개선 및 해결 조치 (Solutions Implemented)

### 1) `local-server/google_drive_gas.js` (v2.2 개선)
* **`VIDEO_FOLDER_ID` 일치화**: `GOOGLE_VIDEO_FOLDER_ID` (`1RgvKVq-J7JItVRD6M_9asnU8NfnaQ_dU`)와 정확히 일치하도록 수정.
* **`action: "cleanup"` 지원 추가**: `doPost`에서 `action === "cleanup"` 파라미터 수신 시 `cleanupOldFiles()`를 원격 실행하여 로컬 서버의 요청으로도 클라우드 파일이 즉시 파기되도록 기능 확장.

### 2) `local-server/app.py` 스토리지 & 클린업 로직 고도화
* **GAS 원격 파기 동기화**: `cleanup_expired_files()` 실행 시 로컬 `uploads/` 24시간 파기와 함께 GAS Webhook에 `{"action": "cleanup"}`을 전송하여 클라우드 파기 동시 실행.
* **불필요한 403 에러 로그 억제**: 서비스 계정이 타인 소유 파일 삭제 시 발생하는 `insufficientFilePermissions` 에러를 조용히 무시하고 GAS 트리거로 위임.
* **0-Quota 에러 안내 개선**: 서비스 계정 업로드 실패 시 무의미한 에러 대신 친절한 안내 메시지(`파일은 로컬 'uploads/'에 안전하게 보관되어 모바일 즉시 다운로드 가능`)를 출력.

---

## 4. 검증 결과

1. **로컬 파일 파기**: 24시간 지난 로컬 캐시 파일 정상 삭제 확인.
2. **클라우드 파일 파기**: GAS 시간 기반 트리거 및 원격 `cleanup` 요청을 통해 사용자 계정 소유권으로 안전하게 휴지통 이동(파기) 완료.
3. **스마트폰 다운로드 UX**: 로컬 `uploads/` 및 ngrok 프록시를 통해 구글 드라이브 오류 여부와 무관하게 이용자에게 100% 1-Click 다이렉트 다운로드 제공 보장.
