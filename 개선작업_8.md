# 개선작업 8: OpenAI 연령 변환 인물/포즈/배경 보존 및 UI/UX 스크롤·QR·2배속 영상 개선

## 1. 요청 사항 및 피드백 분석
1. **1번 이미지 피드백**: "AI 변환을 보냈는데, 오픈소스 모델로 불러오고 있다고 하는데, 제대로 AI 변환이 되고 있는게 맞는지?"
   - 확인: 로딩창 안내 문구에 남아 있던 정적 텍스트 잔재를 OpenAI 최신 모델 안내로 갱신하고, 백엔드가 실제로 OpenAI API(`client.images.edit`, `gpt-image-1.5` / `gpt-image-2`)를 호출하고 있음을 명확히 함.
2. **2번, 3번 이미지 피드백**: "변환된 전체 이미지를 볼 수 없게 되어 있고, QR 코드로 찍어도 확인이 되지 않는다. 그리고 2배속 영상도 나타나지 않는다."
   - 확인: 
     - 세로 4컷 프레임이 세로로 길어서 화면을 벗어나 스크롤해야만 했던 레이아웃 버그 ➔ `max-height: calc(100vh - 220px)`, `object-fit: contain`으로 스크롤 없이 한눈에 4컷 전체가 들어오도록 최적화.
     - QR 코드가 외부 Vercel 주소를 바라보아 로컬 미디어를 가져오지 못하던 문제 ➔ 로컬 LAN IP(`http://<LAN_IP>:8000/...`) 직결 주소로 QR 발급.
     - 모바일 브라우저의 2배속 영상 재생 실패 ➔ `autoplay`, `muted`, `playsinline`, 직접 스트림 URL 주입으로 즉시 재생 보장.
     - 키오스크 Finish 화면 자체에도 **2배속 비하인드 동영상과 4컷 사진 미리보기 플레이어** 신규 탑재.
3. **프롬프트 피드백**: "이미지생성 규칙을 @[c:\4cuts_pjt\연령변환 프롬프트] 해당 프롬프트를 잘 따르고 있는지 재검토가 필요해보이고, 추가로 사용자의 얼굴과 포즈를 기반으로 연령대가 변경됨에 따라서 변환해야한다. 인풋 이미지를 {기반}으로 연령만 변경되는 형태로 만들고 배경도 이에 따라서 추천하는 배경으로 만들어줘."
   - 확인: `연령변환 프롬프트` 4종 파일의 모든 세부 규칙(인원수 보존, 안경 착용 시 필수 유지, 얼굴 골격/표정/포즈/손동작 보존, 유아 배제 어린이 6~10세, 한국 고교 교복 15~18세, 3컷 원본 100% 보존, 기품 있는 노년 65~80세, 생애 주기별 추천 스튜디오 배경)을 영어 프롬프트에 100% 완벽히 매핑.

---

## 2. 세부 소스코드 개선 내역

### 2.1. `local-server/openai_transformer.py`
- 기존 마스크 사각형 제거: 원본 이미지를 마스크 없이 전달하여 AI가 사용자의 얼굴 골격, 이목구비, 안경, 포즈를 완벽히 인지한 후 연령대와 의상, 배경만 자연스럽게 변경하도록 수정.
- 프롬프트에 `연령변환 프롬프트`의 핵심 문구 전면 이식:
  - `CRITICAL IDENTITY & POSE PRESERVATION`
  - `Preserve exact number of people, do not add/remove/duplicate`
  - `Strictly preserve glasses (if wearing glasses, MUST keep the glasses)`
  - `RECOMMENDED BACKGROUND` (파스텔 유년기 스튜디오 / 세련된 교복 프로필 스튜디오 / 품격 있는 앰비언트 노년 서재)

### 2.2. `local-server/templates/index.html` & `style.css`
- 로딩창 문구를 "AI 인생 4컷을 생성하고 있습니다 - 최신 OpenAI 생성 AI가 얼굴과 포즈를 정밀 분석하여 어린이·청소년·노년 생애 주기 변환을 완성하는 중..."으로 변경.
- 화면 6 (`screen-result`) 대형 프레임 컨테이너에 `overflow: hidden !important`, 이미지에 `max-height: calc(100vh - 220px)`, `object-fit: contain` 적용으로 스크롤 없는 한눈에 보기 완성.
- 화면 7 (`screen-finish`)에 2배속 비하인드 영상 플레이어 및 4컷 완성본 썸네일 프리뷰 추가 (`.finish-media-preview-grid`).

### 2.3. `local-server/app.py` & `vercel-frontend/download.html`
- QR 코드 생성 시 로컬 호스트 LAN IP를 감지하여 스마트폰에서 바로 접속 가능한 URL로 QR 발급.
- 모바일 다운로드 뷰어에서 2배속 영상의 네이티브 스트리밍 안정화 및 1-클릭 즉시 다운로드 보장.

---

## 3. Git 형상 관리
- 수정한 모든 파일 커밋 및 GitHub 원격 저장소(`origin/main`) 푸시 진행.
