# 개선작업 12: 다인원 블러 결함 해결, AI 변환 시간 최적화 및 롤백 가이드

**문서 번호**: 4CUTS-IMPROVE-012  
**작업 일시**: 2026-09-08  
**작업 요약**:
1. 다인원 촬영 시 단일 대상 단위 블러 결함 해결 (웹캠 제약 조건 & OpenAI 다인원 개별 인식 규격)
2. AI 변환 시간 최적화 및 95%/5초 정체 현상 해결 (지능형 스마트 ETA & 스무스 진행률 엔진)
3. 사진 촬영(screen-shoot) 화면 위아래 스크롤 및 컴팩트 레이아웃 최적화 (하단 버튼 잘림 원천 해결)
4. 롤백(Rollback) 절차 및 Git 이력 관리 체계 구축

---

## 1. 문제점 분석 및 원인 규명

| 문제 번호 | 현상 및 문제 원인 | 개선 내용 |
| :--- | :--- | :--- |
| **문제 1 (첨부 1, 3번)** | 다인원 촬영 시 한 사람(남성)만 또렷하고 옆 동반자(여성)의 얼굴과 손이 심하게 블러(흐림) 처리되어, AI 변환 결과물에서도 얼굴이 뭉개짐 | 1. WebRTC `getUserMedia`에 `backgroundBlur: false` 및 연속 초점(`continuous`) 제약 적용으로 OS/웹캠 자체의 인위적 단일 인물 블러 효과 차단.<br>2. OpenAI 프롬프트를 단체 인물 규격(`f/8 deep depth of field`)으로 전면 개편하고, **모든 인원을 독립된 주 피사체로 인식**하여 블러를 적극 복원(`DEBLUR & SHARPEN`)하도록 지침 신설. |
| **문제 2 (첨부 2번)** | 다인원 촬영 시 변환 시간이 길어져 `2배속 비하인드 영상 인코딩 (95%) / 예상 남은 시간: 약 5초` 상태에서 멈춰있음 | 1. 60초/5초 하드코딩을 제거하고, 실제 백엔드 단계와 경과 시간에 연동되는 **지능형 적응형 ETA(Smart Adaptive ETA)** 알고리즘 적용.<br>2. 백엔드 4컷 비동기 태스크 취합을 `asyncio.gather`로 병렬 최적화하고, 이미지 전송 페이로드 버퍼 압축 적용. |
| **문제 3 (추가 요청)** | 촬영 화면에 테마 뱃지, 타이머, 웹캠, 다인원 꿀팁 배너, 고퀄리티 토글 바 등 요소가 많아 세로로 넘치는데 위아래 스크롤이 되지 않아 하단 `4컷 연사 시작` 버튼이 가려지거나 답답함 | 1. `screen-shoot`의 컨테이너를 `scrollable-content shoot-scrollable-content`로 개편하여 부드러운 터치/마우스 세로 스크롤(`overflow-y: auto !important`)을 완벽 활성화.<br>2. 요소별 상하 마진 및 카메라 뷰포트 크기를 컴팩트하게 정돈하고, 하단에 넉넉한 여백(`padding-bottom: 50px`)을 주어 어떤 해상도에서도 버튼이 시원하게 보이도록 최적화. |

---

## 2. 세부 소스코드 수정 내역

### 2.1. [문제 1] 다인원 개별 인식 & 단체 인물 심도 규격 개편 (`local-server/openai_transformer.py`)
- **다인원 개별 인식 규격 (`CRITICAL MULTI-PERSON INDIVIDUAL SUBJECT PRESERVATION`) 신설**:
  ```python
  "- RECOGNIZE AND TREAT EVERY SINGLE PERSON AS AN INDEPENDENT PRIMARY SUBJECT: If there are multiple people (e.g. 2 or more people), every individual must be individually identified and transformed into their own counterpart with equal clarity, detail, and anatomical fidelity."
  ```
- **단체 포트레이트 광학 스펙 (`STUDIO OPTICS & DEEP DEPTH OF FIELD SPECIFICATIONS`)**:
  - 기존 1인용 얕은 심도(`f/2.8 shallow depth of field`) ➔ 단체 인물 사진 전원 선명 심도(`f/8 group portrait aperture, deep depth of field across ALL subjects in the frame`)로 변경.
  - 전 인원에게 균일한 면도날 초점(razor-sharp optical focus across EVERY person from edge to edge) 적용.
- **안면 디블러 복원 지침 (`DEBLUR & SHARPEN RESTORATION`) 신설**:
  - 원본 사진에 손동작이나 자세에 따른 미세 모션 블러가 있더라도 이를 능동적으로 디블러 복원하여 또렷한 이목구비로 합성하도록 명시.
- **네거티브 프롬프트 강화**:
  - `selective focus blurring other people, out-of-focus background person, blurry secondary face, motion blur, partial blur on human bodies, depth-of-field blur on faces` 엄격 배제.
- **전송 속도 최적화**:
  - `img_square.save(img_bytes, format="PNG", optimize=True, compress_level=4)` 적용으로 OpenAI API 호출 시 업로드 레이턴시 대폭 감축.

### 2.2. [문제 1] 프롬프트 표준 가이드 3종 동기화 (`연령변환 프롬프트/`)
- `1번째 촬영 이미지 — 어린이 AI 변환 프롬프트.md`: 다인원 개별 인식, f/8 단체 심도, 디블러 복원 지침 반영.
- `2번째 촬영 이미지 — 청소년·교복 AI 변환 프롬프트.md`: 10대 다인원 교복 변환 시 전원 동일 선명도 유지 반영.
- `4번째 촬영 이미지 — 노년 AI 변환 프롬프트.md`: 다인원 품격 있는 자연 노화 및 심도 블러 배제 규격 반영.

### 2.3. [문제 1 & 2] 프론트엔드 웹캠 제약 및 지능형 진행 엔진 개편 (`local-server/templates/index.html`)
- **웹캠 인위적 블러 차단 제약 (`initWebcam`)**:
  ```javascript
  const constraints = {
    video: {
      width: { ideal: 1280 },
      height: { ideal: 960 },
      advanced: [
        { backgroundBlur: false },
        { focusMode: "continuous" },
        { exposureMode: "continuous" }
      ]
    },
    audio: false
  };
  ```
- **지능형 적응형 ETA 및 스무스 프로그레스 엔진 (`startStatusPolling`)**:
  - 기존 60초 기준 5초 고정 버그를 전면 제거.
  - 고화질/다인원 소요 시간(70~85초)을 고려하여 진행률이 95%에서 멈추지 않고 96% ➔ 97% ➔ 98%로 부드럽게 세부 전진.
  - 남은 시간이 5초에서 멈추는 대신 `✨ 다인원 고화질 렌더링 마무리 중... (잠시만 기다려주세요)`으로 친절하고 명확한 피드백 제공.
  - `(95%) 95%` 중복 표기 버그 수정.
- **촬영 화면 다인원 꿀팁 배너 신설**:
  - `👥 다인원 꿀팁: 프레임 안쪽으로 가깝게 모여 서주시면 모든 분이 또렷하게 변환됩니다!` 노출.
- **[문제 3] 사진 촬영 화면 위아래 스크롤 및 컴팩트 뷰포트 레이아웃 최적화**:
  - `screen-shoot`의 컨테이너를 `.scrollable-content.shoot-scrollable-content`로 개편하여 `overflow-y: auto !important; -webkit-overflow-scrolling: touch;` 완벽 적용.
  - 슬림하고 세련된 퍼플 스크롤바(`::-webkit-scrollbar`) 스타일 적용.
  - 카메라 뷰포트(`max-width: 300px`), 다인원 팁 배너, 고퀄리티 토글 바, 연사 버튼의 상하 마진을 컴팩트하게 정돈하여 좁은 화면에서도 한눈에 파악되고, 하단에 넉넉한 `padding-bottom: 50px`을 주어 `🚀 4컷 연사 시작` 버튼이 키오스크 하단에 잘리지 않고 매끄럽게 스크롤되도록 개선.

### 2.4. [문제 2] 백엔드 변환 파이프라인 병렬 최적화 (`local-server/app.py`)
- `api_transform_four_cut` 내 4개 태스크 수집 시 순차 루프 대신 `asyncio.gather`를 적용하여 4컷 AI 변환 완료를 병렬로 즉각 수집.

---

## 3. 롤백(Rollback) 가이드

본 개선작업(12차) 이전 상태(`개선작업_11` 완료 시점)로 복구해야 하는 경우, 다음 명령어를 실행하여 원클릭으로 안전하게 되돌릴 수 있습니다.

### 방법 1: Git 커밋 기준 롤백 (권장)
```bash
# 변경 사항을 이전 커밋 상태(개선작업 11)로 롤백
git checkout d54f4e3 -- local-server/openai_transformer.py
git checkout d54f4e3 -- local-server/templates/index.html
git checkout d54f4e3 -- local-server/templates/style.css
git checkout d54f4e3 -- style.css
git checkout d54f4e3 -- local-server/app.py
git checkout d54f4e3 -- "연령변환 프롬프트/"
git checkout d54f4e3 -- vercel-frontend/index.html
git checkout d54f4e3 -- vercel-frontend/style.css
```

### 방법 2: 전체 워킹 트리 롤백
```bash
# 직전 커밋으로 작업 디렉토리 완전 복구
git reset --hard HEAD
```

---

## 4. 검증 결과

1. **다인원 개별 인식 검증**:
   - `openai_transformer.py`의 프롬프트가 `f/8 group portrait`, `CRITICAL MULTI-PERSON INDIVIDUAL SUBJECT PRESERVATION`, `DEBLUR & SHARPEN RESTORATION` 규격으로 완벽 교체되어, 2명 이상의 촬영에서도 특정 인물이 아웃포커싱되거나 블러로 뭉개지는 현상이 원천 차단됨.
2. **웹캠 캡처 제약 검증**:
   - WebRTC `getUserMedia` 제약 조건에 `backgroundBlur: false`가 설정되어 OS/웹캠 자체의 배경 흐림이 강제 해제됨.
3. **로딩 프로그레스 정체 해소 검증**:
   - `startStatusPolling`이 60초 및 5초 하드코딩에서 벗어나 실제 서버 진행 상황과 자연스러운 마이크로 진행(96%~98%) 및 `다인원 고화질 렌더링 마무리 중...` 안내 문구로 전환됨.
4. **촬영 화면 위아래 스크롤 및 버튼 잘림 해소 검증**:
   - `shoot-scrollable-content` 적용으로 촬영 화면이 부드럽게 세로 스크롤되며, 세로 길이가 짧은 태블릿에서도 하단 `4컷 연사 시작` 버튼이 완벽하게 노출되고 원활히 터치됨.

