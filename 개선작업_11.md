# 개선작업 11: UI 미감 개선 및 인쇄 프레임 인물 크롭 왜곡 해결

**문서 번호**: 4CUTS-IMPROVE-011  
**작업 일시**: 2026-09-07  
**작업 요약**: 
1. AI 변환 로딩 화면 간소화 (불필요한 텍스트 리스트 제거, 남은 시간/게이지/완료 이벤트 중심)
2. 화면 7(Finish) 하단 액션 버튼 2단 그리드 개편 (텍스트 줄바꿈/삐져나옴 원천 해결)
3. 엽서 인화 프레임 인물 얼굴 강제 확대 크롭 해결 (실제 촬영 전체 이미지 100% 보존 Aspect Fit)

---

## 1. 문제점 분석 및 개선 요구사항

| 문제 번호 | 현상 및 문제 원인 | 개선 내용 |
| :--- | :--- | :--- |
| **문제 1 (첨부 1번)** | 로딩 오버레이 화면에 5단계 생애주기 스텝, 자잘한 상태 텍스트, 팁 박스 등 불필요한 메시지가 과도하게 나열되어 산만함 | 수많은 텍스트를 전면 제거하고, **진행률 게이지 바 + 예상 남은 시간 + "✨ 변환 완료!" 이벤트성 축하 배너** 중심의 미니멀 프리미엄 UI로 개편 |
| **문제 2 (첨부 2번)** | 화면 7(Finish) 하단 버튼 3개가 1행에 억지로 배치되어 `◀ 4컷 다시보기 (크게)` 텍스트가 줄바꿈되고 버튼 밖으로 삐져나옴 | **2단 레이아웃(2-Row Grid)**으로 전면 재설계. 상단 1열에 `[ ◀ 4컷 다시보기 ]` (50%) + `[ 🖨️ 엽서 인화 출력 ]` (50%), 하단 2열에 `[ 🏠 처음으로 (새로운 촬영) ]` (전폭) 배치 및 `white-space: nowrap` 적용 |
| **문제 3 (첨부 3번)** | 엽서 인쇄본에서 인물 얼굴만 줌인되어 나오고 상체, 어깨, 옷, 손동작 등이 45% 이상 잘려나감 | `concept_transformer.py`의 강제 크롭(`img.crop`) 로직을 완전히 제거하고, **촬영된 실제 전체 이미지가 100% 손실 없이 슬롯 중앙에 나타나는 Aspect Fit (Contain)** 알고리즘 적용 |

---

## 2. 세부 소스코드 수정 내역

### 2.1. [문제 1] 로딩 화면 미니멀 개편 (`templates/index.html`, `style.css`)
- **불필요한 DOM 제거**: `#timeline-stepper` (1~5단계 스텝 리스트), `#loading-tip-box` (팁 줄글) 전면 삭제.
- **간결한 게이지 & 남은 시간**:
  - `#loading-main-title`: `AI 인생 4컷 변환 중...`
  - `#progress-bar-fill` & `#progress-percent-text`: 부드러운 퍼센트 게이지
  - `#loading-timer-remaining`: `⏳ 예상 남은 시간: 약 OO초`
- **변환 완료 이벤트성 배너 신설**:
  - 100% 도달 시 `#loading-complete-banner` (`🎉 AI 4컷 변환 완료!`) 팝업 애니메이션 표출 후 0.8초 뒤 결과 화면으로 전환.

### 2.2. [문제 2] 화면 7 하단 버튼 UI/미감 개편 (`templates/index.html`, `style.css`)
- **2단 키오스크 전용 액션 그리드**:
  ```html
  <footer class="fixed-footer finish-fixed-footer">
    <div class="finish-action-grid">
      <div class="finish-dual-btn-row">
        <button id="btn-back-to-result" class="btn btn-action-dual btn-back-dual">
          ◀ 4컷 다시보기
        </button>
        <button id="btn-print-modal" class="btn btn-action-dual btn-print-dual">
          🖨️ 엽서 인화 출력
        </button>
      </div>
      <button id="btn-reset-home" class="btn btn-home-full">
        🏠 처음으로 (새로운 촬영)
      </button>
    </div>
  </footer>
  ```
- **CSS 스타일**: `white-space: nowrap !important;`, 넉넉한 14px 라운딩 및 보라색/그린 그라디언트 섀도우 적용으로 글자 잘림을 영구 방지하고 고급스러운 터치 피드백 부여.

### 2.3. [문제 3] 엽서 인쇄 프레임 전체 이미지 100% 보존 (`concept_transformer.py`)
- **원인 분석**:
  - 카메라 캡처는 세로형 $4:5$ ($640 \times 800$, ratio 0.8) 또는 OpenAI $1024 \times 1024$ (ratio 1.0)인데, 기존 슬롯이 가로형 $490 \times 345$ (ratio 1.42)로 고정되어 상하 45%를 잘라내어 얼굴만 확대되었음.
- **해결 알고리즘 (Aspect Fit Contain)**:
  ```python
  # 촬영된 실제 전체 이미지가 손실 없이 100% 보이도록 Aspect Fit 적용
  scale = min(photo_w / img_w, photo_h / img_h)
  new_w = int(img_w * scale)
  new_h = int(img_h * scale)
  img_resized = img_rgb.resize((new_w, new_h), Image.Resampling.LANCZOS)

  # 슬롯 정중앙에 배치 (어깨, 옷, 손, 얼굴 전체 완벽 보존)
  paste_x = slot_x + (photo_w - new_w) // 2
  paste_y = slot_y + (photo_h - new_h) // 2
  frame.paste(img_resized, (paste_x, paste_y))
  ```
- **결과**: 3컷(현재 원본)의 상체, 어깨, 옷, 손동작은 물론 1, 2, 4컷(AI 변환)까지 **어떤 크롭 손실도 없이 실제 촬영 구도 그대로 완벽 출력**됨.

---

## 3. 검증 결과
- ✅ 로딩 화면에서 장황한 메시지가 사라지고 심플한 게이지와 남은 시간, 완료 배너만 깔끔하게 노출됨.
- ✅ Finish 화면 하단 버튼의 텍스트가 줄바꿈되거나 삐져나오지 않고 50:50 듀얼 + 풀 위드로 단정하게 배치됨.
- ✅ 1200x1800 엽서 프레임 생성 시 인물 얼굴만 확대되던 문제가 해결되어 상반신 전체가 온전히 렌더링됨.
