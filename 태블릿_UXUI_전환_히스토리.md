# 태블릿 전용 UX/UI 전환 히스토리

## 📌 기획 배경

| 항목 | 내용 |
|------|------|
| **작업일** | 2026-09-17 |
| **대상 디바이스** | 갤럭시 탭 S7+ (13인치, 2800×1752) |
| **화면 비율** | 16:10 (가로 모드 고정) |
| **이전 상태** | 모바일 세로 비율 (max-width: 440px, 세로 스크롤) |
| **목표** | 태블릿 가로 모드에 최적화된 2열 분할 레이아웃 |

---

## 📐 UX/UI 레퍼런스 매핑

| 태블릿 레퍼런스 폴더 | 화면 ID | 설명 |
|---------------------|---------|------|
| `4_13` | screen-home | 홈 화면 (좌 5:우 7 분할) |
| `4_13_1_1` | screen-filter | 컨셉 선택 (좌 캐러셀:우 확인 패널) |
| `4_4_13` | screen-shoot | 촬영 부스 (좌 8:우 4 분할) |
| `4_ai_13_4_qr` | screen-finish | 완료/QR (좌 5:우 7 분할) |

---

## 🔧 변경 내역

### 1. CSS 전면 개편 (`style.css`)

**컨테이너 변경:**
- 기존: `max-width: 440px`, 세로 스크롤 기반
- 변경: `@media (min-width: 900px) and (orientation: landscape)` 미디어 쿼리에서 풀스크린 16:10, `overflow: hidden`

**태블릿 전용 레이아웃 (미디어 쿼리 내):**
- 화면 1 (홈): `grid-template-columns: 5fr 7fr` - 좌측 일러스트/타이틀, 우측 가이드/CTA
- 화면 2 (필터): `grid-template-columns: 1fr 1fr` - 좌측 캐러셀, 우측 선택 확인
- 화면 3 (촬영): `grid-template-columns: 8fr 4fr` - 좌측 카메라, 우측 진행도/버튼
- 화면 4 (확인): `grid-template-columns: repeat(4, 1fr)` - 4컷 가로 나열
- 화면 5 (결과): `grid-template-columns: 5fr 7fr` - 좌측 스트립, 우측 프레임/버튼
- 화면 6 (완료): `grid-template-columns: 5fr 7fr` - 좌측 미디어, 우측 QR/완료

**글꼴 스케일업:**
- 타이틀: 19px → 24px
- 본문: 12px → 14~16px
- 버튼: 15px → 18~24px

### 2. HTML 구조 변경 (`index.html`)

**추가된 래퍼 div:**
- `.home-left-col` / `.home-right-col` - 홈 화면 좌우 분할
- `.shoot-left-col` / `.shoot-right-col` - 촬영 화면 좌우 분할
- `.finish-left-col` / `.finish-right-col` - 완료 화면 좌우 분할

**콘텐츠 이동:**
- 홈 화면의 `service-free-badge`를 헤더에서 `.home-left-col`로 이동
- 완료 화면에 레퍼런스 기준 완료 타이틀 배너 추가
- QR 카드에 다운로드 가능 항목 태그 추가 (4컷 분할본, GIF 영상)

### 3. 유지된 사항
- **JavaScript 비즈니스 로직 변경 없음** - 화면 전환, 카메라, API 연동, 캐러셀, 인화 모달 등
- **기존 디자인 시스템 유지** - 두들/네오브루탈리즘 스타일
- **모바일 하위 호환** - 래퍼 div는 모바일에서는 기본 display:block으로 세로 스택 유지

---

## ⚠️ 주의사항

1. 태블릿 가로 모드(900px 이상 + landscape)에서만 2열 레이아웃 활성화
2. 모바일(세로)에서는 기존 레이아웃 그대로 유지
3. `overflow: hidden`으로 각 화면이 스크롤 없이 한 화면에 렌더링
4. 웹캠 `aspect-ratio: unset`으로 태블릿에서 카메라 영역 최대 활용

---

## 📂 관련 파일

- `vercel-frontend/style.css` - 태블릿 레이아웃 CSS
- `vercel-frontend/index.html` - 태블릿 레이아웃 HTML
- `태블릿 전용 UXUI 레퍼런스/` - 디자인 레퍼런스 폴더

---

## 🚀 2차 추가 기능 및 UX 최적화 작업 (2026-09-17)

| 번호 | 요구사항 | 조치 내용 |
|---|---|---|
| **1** | **01, 02, 03, 04 숫자 제거** | • 컨셉 선택 카드 내부 셀 라벨에서 불필요한 번호 접두어 제거 (`01 왕` ➔ `왕`, `01 픽셀 변환` ➔ `픽셀 변환`, `01 맑은 톤` ➔ `맑은 톤` 등)<br>• 카메라 메타 라벨 `CAM 01` ➔ `CAM`으로 정돈 |
| **2** | **전체 화면 공통 뒤로가기 버튼** | • 네오브루탈리즘 스타일의 `btn-header-back` 공통 컴포넌트 추가<br>• 컨셉 선택, 촬영 중, 사진 확인, 프레임/AI 결과, 최종 완료 각 화면 헤더에 `‹ 뒤로` 버튼 완비<br>• 촬영 중 뒤로가기 시 웹캠 스트림 및 카운트다운 타이머 즉시 정지(안전 취소) 로직 적용 |
| **3** | **촬영 시간 선택 (3초 ➔ 7초, 10초)** | • 우측 컨트롤 패널에 `⏱️ 촬영 타이머` 카드 추가<br>• `7초 (권장)` 및 `10초 (여유)` 선택 칩 버튼 제공<br>• 선택된 타이머 값에 따라 상단 서브 배지 및 카운트다운 숫자 동적 갱신 및 촬영 시퀀스 연동 |
| **4** | **테두리 변경 시 AI 변환 사진 유실 방지 최적화** | • `aiPreviewEl`의 AI 프레임 URL을 `savedAiFrameUrl`에 안전하게 캐싱<br>• 이미지 로딩 실패/에러 발생 시 캐시된 AI 프레임 자동 롤백 핸들러 구축<br>• 백엔드(`app.py`) `update-frame-color`에서 AI 변환 사진이 없을 때 원본으로 덮어쓰지 않고 기존 AI 프레임 이미지를 영구 보존하도록 방어 로직 강화 |
| **5** | **테두리 색상 '흰색(White)'만 남김** | • 기존 블랙/레드/네이비 색상 버튼을 제거하고 `⚪ 화이트 (기본)` 단일 버튼으로 간소화하여 이미지 깨짐 및 불필요한 재호출 방지 |
| **6** | **최종 인화본 뱃지 및 동그란 도장(通) 제거** | • 결과 프리뷰 스트립 카드 상단의 `.stamp-tag-top` ("최종 인화본 ✨")과 `.stamp-seal-right` ("通") 요소 완전 삭제 |
