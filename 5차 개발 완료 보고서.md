# 5차 개발 완료 보고서 (오류 수정 & 모바일 UI 개선 & 데이터 자동 파기 강화)

## 1. 개요
본 보고서는 4컷 사진 부스 프로젝트의 5차 개발 및 오류 수정 완료 내역을 정리합니다. 
이번 단계에서는 **Google Apps Script 403 Forbidden 에러 조치**, **구글 드라이브 실패 시 데이터 저장 위치 안내**, **모바일 다운로드 뷰어(`download.html`)의 탭 방식 UI 개선 및 비하인드 동영상 재생 문제 해결**, 그리고 **24시간 데이터 자동 파기 스케줄러 강화** 작업을 집중적으로 수행했습니다.

---

## 2. 주요 수정 및 조치 내역

### 2.1. Google Drive GAS 403 Forbidden 에러 원인 및 해결
- **문제 현상:**
  - 백엔드 서버에서 GAS 업로드 시 `HTTP Error 403: Forbidden` 발생.
  - 연이어 Fallback으로 실행된 Service Account 업로드 시도도 `Service Accounts do not have storage quota` (Quota 0 byte 정책 제한) 403 오류 발생.
- **원인 분석:**
  1. **GAS 배포 권한 미흡:** Google Apps Script 웹 앱 배포 시 `액세스 권한이 있는 사용자` 설정이 **'모든 사용자 (Anyone)'**가 아닌 '나만' 또는 'Google 계정이 있는 사용자'로 설정되어 있으면 외부 HTTP 요청이 거부(403)됩니다.
  2. **Python HTTP 요청 헤더 누락:** 기존 `urllib` 요청 방식에서 구글 보안 정책으로 인해 브라우저 헤더(`User-Agent`)가 없거나 302 리디렉션 처리 시 요청이 차단되는 문제 발생.
- **조치 사항:**
  - `local-server/app.py`의 `upload_to_google_drive` 함수를 `requests` 라이브러리와 `User-Agent` 헤더 기반으로 리팩토링하여 리디렉션 및 연동 내구성을 대폭 강화했습니다.
  - **사용자 필수 가이드:** 구글 앱스 스크립트 배포 시 [배포] -> [새 배포] -> 액세스 권한을 **"모든 사용자 (Anyone)"**로 지정하여 배포해야 403 에러가 해결됩니다.

---

### 2.2. 사진 및 영상 데이터 저장 위치 안내 (Data Storage Location)
- **질문 답변:** 구글 드라이브 업로드가 실패하더라도 모바일에서 다운로드가 가능한 이유:
- **저장 위치:**
  - 백엔드는 구글 드라이브 업로드가 실패하더라도 로컬 백엔드 서버의 `local-server/uploads/` 폴더에 촬영 사진(`ai_frame_*.jpg`, `orig_frame_*.jpg`)과 비하인드 동영상(`behind_video_*.webm`)을 안전하게 저장합니다.
  - QR 코드로 모바일 페이지 접속 시 `download.html`이 백엔드의 ngrok URL(`&srv=...`)을 통해 로컬 서버의 `uploads/` 경로에서 직접 미디어 파일(Blob)을 수신하므로, 구글 드라이브 업로드 여부와 상관없이 스마트폰에서 고화질로 즉시 다운로드할 수 있습니다.

---

### 2.3. 모바일 다운로드 뷰어(`download.html`) UI/UX 개선 (스크롤 및 영상 표시 문제 해결)
- **문제 현상:** 모바일 접속 시 세로로 길게 연결된 4컷 사진으로 인해 화면 아래에 위치한 비하인드 동영상을 보려면 긴 스크롤이 필요하고, 일부 모바일 브라우저에서 동영상 플레이어가 정상 렌더링되지 않던 문제.
- **개선 사항:**
  1. **모바일 탭 컨트롤 도입 (`[ 📸 4컷 사진 ]` | `[ 🎥 비하인드 영상 ]`):** 상단 탭을 통해 한 번의 터치로 4컷 사진과 비하인드 동영상을 손쉽게 교체하며 감상 및 다운로드할 수 있도록 UI를 개편했습니다.
  2. **동영상 플레이어 로딩 안정화:** HTML5 `<video>` 태그 속성에 `playsinline`, `webkit-playsinline`, `preload="metadata"`를 적용하고, JavaScript에서 동영상 Blob URL 매핑 및 `video.load()` 호출 로직을 보강하여 모바일(iOS Safari, Android Chrome)에서 바로 재생 가능하도록 수정했습니다.
  3. **전체 한 번에 보기 기능 유지:** 필요 시 하단의 '📄 사진과 영상 모두 한 번에 보기' 버튼을 터치해 스크롤 형태로도 볼 수 있습니다.

---

### 2.4. 24시간 이내 데이터 자동 파기 프로세스 구축 및 검증 (No-DB & 개인정보 보호)
- **자동 파기 프로세스:**
  - `local-server/app.py`에 `APScheduler` 기반의 `cleanup_expired_files()` 자동화 로직이 탑재되어 있습니다.
  - **주기적 검사:** 15분 간격(`interval`, `minutes=15`)으로 로컬 `uploads/` 폴더 및 구글 드라이브 지정 폴더를 자동 스캔합니다.
  - **파기 조건:** 파일 생성 시간이 24시간(86,400초)을 초과한 경우 로컬 디스크 및 구글 드라이브에서 `os.remove()` 및 API 삭제를 수행하여 **영구 파기**합니다.
  - **서버 시작 시 즉시 파기 실행 보강:** 이번 업데이트에서 백엔드 서버 가동 시점에도 즉시 만료 파일 검사를 1회 수행하도록 로직을 강화했습니다.

---

## 3. 종합 시스템 현황 및 확인 권장 사항

| 항목 | 개선 및 상태 | 비고 |
| :--- | :--- | :--- |
| **GAS 드라이브 연동** | Python `requests` & `User-Agent` 적용 완료 | GAS 배포 권한 **'모든 사용자(Anyone)'** 설정 필요 |
| **데이터 저장 fallback** | `local-server/uploads/` 저장 후 ngrok 서빙 | 클라우드 실패 시에도 데이터 손실 없음 |
| **모바일 다운로드 UI** | 탭(Tab) 기반 사진/영상 분리 UI 개편 완료 | 스크롤 없이 동영상 탭 터치로 즉시 확인 |
| **24시간 데이터 파기** | APScheduler 15분 주기 & 서버 시작 시 파기 실행 | 개인정보보호법 준수 영구 자동 파기 |

---
**작성일:** 2026-08-25  
**작성자:** Antigravity AI Assistant
