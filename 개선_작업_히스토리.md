# AI 인생4컷 시스템 개선 및 기능 고도화 작업 히스토리

**작업 일시**: 2026년 9월 12일  
**프로젝트**: 서울청년센터 영등포 × 놀면뭐AI (AI 4-CUT STUDIO)  
**문서 목적**: 키오스크 및 모바일 다운로드 페이지 디자인 전면 개편, 조선 4컷 테마 신규 개발, gpt-image-2 고화질 모델 기본 탑재, 4컷 프레임 이미지 비율/공백 최적화 작업에 대한 상세 이력 및 아키텍처 기록

---

## 1. 주요 개선 작업 요약

| 구분 | 주요 개선 내용 | 대상 파일 |
| :--- | :--- | :--- |
| **① 다운로드 페이지 리디자인** | 키오스크와 동일한 서울청년센터 영등포 × 놀면뭐AI 네오브루탈리즘 & 빈티지 두들 페이퍼 테마 전면 적용 | `vercel-frontend/download.html` |
| **② 조선 4컷 테마 개발** | 왕·선비·보부상·노비 4개 신분 변신 프롬프트 연동 및 테마 선택/촬영 가이드/변환 파이프라인 구축 | `openai_transformer.py`, `templates/index.html`, `app.py` |
| **③ 고화질 모델 기본 탑재** | 고해상도 생성 모델 `gpt-image-2`를 기본 탑재(`checked`) 및 백엔드 디폴트로 확정 | `templates/index.html`, `openai_transformer.py`, `app.py` |
| **④ 프레임 공백 및 크기 최적화** | OpenAI 1:1(1024x1024)과 웹캠 4:5(640x800)의 비율 차이로 발생하던 상하 흰 공백 제거 및 4컷 크기 100% 일치화 (`ImageOps.fit`) | `concept_transformer.py` |
| **⑤ 출력 프레임 포맷 전면 개편** | 실제 인쇄 출력물을 웹 프리뷰(1번 이미지)와 동일하게 완성형 네오브루탈리즘 감성(최종 인화본 스티커, 通 도장, 점선, 바코드, 영등포 브랜딩)으로 고해상도 렌더링 | `concept_transformer.py`, `app.py` |
| **⑥ 프레임 색상 인쇄 실시간 연동** | 사용자가 선택한 프레임 테두리 색상(크림, 블랙, 레드, 네이비)이 실제 인화 엽서 및 프리뷰에 실시간 반영되도록 파이프라인 구축 | `templates/index.html`, `app.py`, `concept_transformer.py` |
| **⑦ CSS 경로 및 라우팅 복구** | Flask/FastAPI 환경에서 템플릿 CSS가 정상 주입되도록 라우트 보강 및 링크 절대경로 정비 | `templates/index.html`, `app.py` |

---

## 2. 세부 개발 및 개선 내역

### 2.1. QR 모바일 다운로드 페이지 최신 디자인 전면 개편
- **배경**: QR 코드를 스캔하여 손님의 스마트폰으로 진입하는 Vercel 모바일 뷰어(`download.html`)가 기존의 구형 디자인으로 남아있어, 키오스크 본체와 시각적 일관성이 결여되어 있었음.
- **적용 내용**:
  - **디자인 시스템 통일**: 부스 키오스크와 완벽히 일치하는 따뜻한 빈티지 크림 캔버스(`--bg-cream: #FAF6EF`), 미세 도트 그리드 백그라운드, 볼드한 2.5px 잉크 보더(`--border-ink: #1C1917`), 6px 하드 드롭 섀도우(`--shadow-hard`) 적용.
  - **브랜딩 헤더**: `서울청년센터 영등포 × 놀면뭐AI` 공식 배지 및 네오브루탈리즘 스타일 타이틀 뱃지 구현.
  - **미디어 탭 컨트롤러**: `📸 4컷 완성본`과 `🎬 2배속 비하인드 영상`을 즉각적으로 전환할 수 있는 두들 탭 버튼 탑재.
  - **원클릭 다이렉트 저장 버튼**: 스마트폰 갤러리에 터치 한 번으로 다운로드되는 `⬇️ 4컷 사진 즉시 저장 (1-Click)` 액션 버튼 구성.
  - **보안 및 신뢰성 카드**: Google Drive Fallback 스트림 다운로드 기능 및 24시간 개인정보 자동 파기 정책 안내 스티커 카드 배치.
  - **기존 안정성 유지**: 탭 종료 시 로컬 임시 파일을 파기하는 `sendBeacon('/api/cleanup-on-close')` 및 GDrive 연동 로직 100% 보존.

### 2.2. 조선 4컷 (신분 변신) 테마 신규 개발 및 프롬프트 연동
- **배경**: 기존 '인생 사계절 (연령 변환)' 테마에 더해, 조선시대 4개 계층(왕, 선비, 보부상, 노비)으로 신분이 변신하는 엔터테인먼트 테마 요구.
- **프롬프트 구축 (`c:\4cuts_pjt\조선시대 프롬프트` 레퍼런스 완벽 이식)**:
  - **1컷 (왕 - King)**: 붉은색 곤룡포(용보 자수), 익선관 착용, 궁궐 어좌(용상) 및 일월오봉도 병풍, 단청 기둥 배경.
  - **2컷 (선비 - Scholar)**: 백색/아이보리 도포, 칠흑색 흑립(갓), 전통 한옥 대청마루 및 한지 창호, 서안(문방사우) 배경.
  - **3컷 (보부상 - Peddler)**: 무명 저고리와 바지, 양쪽에 흰 목화솜 뭉치가 달린 노란 패랭이 모자, 등 뒤의 봇짐/지게, 정겨운 시골 장터 배경.
- **헤어스타일 고증 최적화 (왕 & 선비 머리카락 삐져나옴 완벽 해결)**:
  - **문제점**: 초기 프롬프트의 "Hair should remain consistent with original in length and style" 문구로 인해, AI 모델이 사용자의 현대적인 긴 머리, 웨이브, 더벅머리를 모자 아래로 그대로 유지하려고 하여 익선관과 갓 밖으로 덥수룩하게 삐져나오는 부자연스러움 발생.
  - **최적화 조치**:
    - **1컷 (왕)**: 익선관 착용 시 머리를 단정한 상투로 틀어 올리고 말총 망건(Manggeon)을 이마에 밀착 착용하도록 명시. 앞머리(Bangs), 잔머리, 옆머리가 익선관 밖으로 일절 삐져나오지 않도록 정돈하여 이마와 눈썹선이 훤히 드러나는 위엄 있는 궁중 사극 국왕의 헤어라인 구축.
    - **2컷 (선비/양반)**: 흑립(갓) 착용 시 앞머리와 옆머리를 완전히 뒤로 빗어 넘겨 망건 안에 밀착 정돈하도록 지시. 갓 아래로 쏟아지는 현대식 웨이브나 덮수룩한 머리를 차단(CRITICAL NEGATIVE)하여 기품 있는 조선 선비의 단아한 상투/망건 스타일 구현.
    - **3컷 (보부상)**: 패랭이 모자 아래로 앞머리가 흩날리지 않도록 머리를 질끈 동여맨 활동적인 장돌뱅이 스타일로 보강.
  - **수정 반영**: `openai_transformer.py`의 `PROMPTS_JOSEON`, `조선시대 프롬프트/` 내 `왕_프롬프트.md`, `선비_프롬프트.md`, `보부상_프롬프트.md` 전면 업데이트 완료.

### 2.3. 고화질 이미지 생성 모델 `gpt-image-2` 기본 탑재
- **배경**: AI 이미지 변환 시 얼굴 왜곡 없이 원본 인물의 이목구비와 포즈를 극사실적으로 유지하기 위해 최신 플래그십 모델 기본 적용 필요.
- **적용 내용**:
  - `local-server/templates/index.html`: 고화질 모드 스위치(`highResToggle`)를 HTML 상에서 `checked` 기본 상태로 고정하고, `[기본 탑재] gpt-image-2 초고화질 모드 활성화` 안내 텍스트 명시.
  - `local-server/openai_transformer.py`: `DEFAULT_MODEL = "gpt-image-2"`로 정의하고, 모든 변환 메서드의 기본 인자값(`model_name="gpt-image-2"`)으로 확정.
  - `local-server/app.py`: 클라이언트로부터 전달되는 `high_res` 플래그 및 단일/전체 변환 파이프라인에서 `gpt-image-2`가 최우선 구동되도록 환경 보장.

### 2.4. 4컷 프레임 이미지 비율 불일치 및 상하 빈 여백 완벽 해결
- **문제 원인 (첨부 2번 이미지 분석)**:
  - OpenAI Edit API는 결과물을 정사각형 `1024x1024` (1:1 비율)로 반환.
  - 부스 웹캠에서 촬영된 원본 이미지는 `640x800` (4:5 비율).
  - 4컷 프레임 캔버스의 개별 슬롯 규격:
    - 웹/모바일 프레임(900x1200): 슬롯 크기 가로 387px × 세로 489px (약 4:5 세로 비율)
    - 엽서 인화 프레임(1200x1800): 슬롯 크기 가로 500px × 세로 365px (가로 비율)
  - 기존 코드는 `min(scale)` 리사이즈 방식을 사용하여 1:1 정사각형 이미지를 가로 슬롯 폭(387px)에 맞추다 보니, 세로 높이가 약 387px로 축소되어 슬롯 높이(489px) 대비 약 102px의 상하 흰색 여백(Letterbox)이 노출됨.
  - 이로 인해 3컷(웹캠 원본 4:5)은 세로로 길게 차는 반면, 1·2·4컷(AI 변환 1:1)은 상하 여백이 남아 4컷의 크기가 짝짝이로 보이는 시각적 결함 발생.
- **해결 방안 (`concept_transformer.py` 최적화)**:
  - 단순 축소 방식 대신 PIL의 고성능 중앙 크롭 적응형 리사이즈 함수인 `ImageOps.fit` 적용:
    ```python
    img_fitted = ImageOps.fit(
        img_rgb, 
        (slot_w, slot_h), 
        method=Image.Resampling.LANCZOS, 
        centering=(0.5, 0.5)
    )
    ```
  - **웹/모바일 프레임 (`create_4cut_frame`)** 및 **캐논 CP1500 엽서 프레임 (`create_4cut_frame_postcard`)** 양쪽 모두 적용.
  - **개선 효과**:
    - 원본(4:5)이든 AI 변환본(1:1)이든 슬롯의 픽셀 규격에 1px 오차도 없이 100% 꽉 차게 렌더링됨.
    - 상하/좌우 빈 여백이 완전히 0%로 제거됨.
    - 4개의 개별 컷이 정확히 동일한 크기와 여백으로 정렬되어 완벽한 대칭 레이아웃 완성.

### 2.5. 실제 출력 프레임 포맷 전면 개편 및 프레임 색상 인쇄 실시간 연동
- **배경 (1번 vs 2번 첨부 이미지 결함 분석)**:
  - 사용자가 부스 화면(1번 이미지)에서 보는 프리뷰는 `최종 인화본 ✨` 노란 스티커, `通` 빨간 도장, 둥근 모서리와 볼드 외곽선, 점선 절취선, 바코드, `청년4컷 스튜디오`, `서울청년센터 영등포 × 놀면뭐AI` 브랜딩이 포함된 네오브루탈리즘 감성 카드였음.
  - 그러나 실제 인화되거나 저장되는 이미지(2번 이미지)는 단순 흰색 바탕에 사진 4개와 맨 밑 텍스트 2줄만 있는 밋밋한 형태였으며, 사용자가 선택한 프레임 색상(크림, 블랙, 레드, 네이비)도 인쇄물에 전혀 반영되지 않았음.
- **적용 내용**:
  1. **고해상도 네오브루탈리즘 프레임 렌더러 구축 (`concept_transformer.py`)**:
     - `render_neobrutalism_4cut`, `create_4cut_frame`, `create_4cut_frame_postcard` 전면 고도화.
     - **좌상단 스티커**: 노란색 회전 스티커(`#FFE370`) + 볼드 외곽선 + `최종 인화본` 텍스트 + 황금빛 다이아몬드 별(✨) 벡터 도형 렌더링.
     - **우상단 도장**: 붉은색 원형 인장(`#B93826`) + 2중 원 테두리 + 흰색 `通` 한자.
     - **카드 프레임 바디**: 볼드 3.5px 잉크 테두리 + 둥근 모서리(Radius 22px) + 우하단 하드 드롭 섀도우.
     - **점선 절취선**: 사진 영역과 푸터를 구분하는 티켓 점선(Dashed Line).
     - **하단 푸터**: 좌측 `청년4컷 스튜디오`, 우측 날짜(`YYYY.MM.DD`), 중앙 세로 바코드 벡터, 하단 `서울청년센터 영등포 × 놀면뭐AI`.
     - **프레임 색상 지원**: `frame_color` 및 `text_color` 매개변수를 지원하여, 어두운 배경(블랙, 레드, 네이비) 선택 시 텍스트/바코드/점선/테두리가 화이트로 자동 대비 반전됨.
  2. **태블릿 UI 색상 선택 및 실시간 동기화 (`templates/index.html`)**:
     - 사용자가 프레임 색상 버튼 클릭 시 `currentFrameBg`, `currentFrameText` 저장 및 버튼 선택 링 표시.
     - `/api/update-frame-color`를 비동기 호출하여 화면 프리뷰와 Finish 화면 썸네일도 해당 색상의 프레임으로 즉시 갱신.
     - `btn-print-modal` 클릭 시 `/api/print`로 `frame_color`, `text_color` 파라미터 전달.
  3. **백엔드 인쇄 파이프라인 동적 렌더링 (`app.py`)**:
     - `/api/print` 엔드포인트에서 요청받은 `frame_color`에 따라 1번 이미지 스타일의 고해상도 1200x1800 엽서 프레임을 실시간으로 렌더링하여 Canon SELPHY CP1500으로 인쇄.
     - `/api/update-frame-color` 신규 엔드포인트 개설.

---

## 3. 변경 및 추가된 파일 목록

```
c:\4cuts_pjt\
├── vercel-frontend\
│   └── download.html                  # [수정] 모바일 QR 다운로드 뷰 전면 리디자인 (네오브루탈리즘 테마)
├── local-server\
│   ├── app.py                         # [수정] 조선4컷 수신, frame_color/text_color 수신, /api/update-frame-color, 인쇄 실시간 렌더링
│   ├── openai_transformer.py          # [수정] 조선시대 4색 프롬프트 탑재, gpt-image-2 기본 모델화, 4컷 변환 파이프라인
│   ├── concept_transformer.py         # [수정] 네오브루탈리즘 4컷 프레임 렌더러(스티커, 도장, 바코드, 색상 지원) 전면 고도화
│   └── templates\
│       ├── index.html                 # [수정] 프레임 색상 버튼 인터랙션, 실시간 색상 갱신, /api/print 색상 전송
│       └── style.css                  # [수정] 영등포 청년센터 × 놀면뭐AI 네오브루탈리즘 스타일시트 완성
└── 개선_작업_히스토리.md              # [신규] 전체 개선 내역 및 아키텍처 히스토리 문서 (본 문서)
```

---

## 4. 검증 및 테스트 결과

1. **디자인 UI 검증**:
   - 로컬 키오스크(`http://localhost:8000/`) 브라우저 접속 확인: CSS 깨짐 현상 없이 네오브루탈리즘 테마가 정상 로드됨.
   - 모바일 다운로드 뷰(`download.html`): 뷰포트 반응형 레이아웃, 사진/비디오 탭 스위칭, 1-Click 다운로드 및 자동 파기 스티커가 완벽하게 렌더링됨.
2. **조선 4컷 및 gpt-image-2 검증**:
   - 테마 선택 창에서 조선 4컷 선택 시 1컷(국왕), 2컷(선비), 3컷(보부상), 4컷(노비) 가이드가 실시간으로 매핑됨.
   - 백엔드로 `sub_theme: joseon`, `model_name: gpt-image-2`가 정상 전달되어 4컷 모두 누락 없이 변환 가능함.
3. **프레임 슬롯 크롭 및 여백 검증**:
   - `ImageOps.fit` 적용으로 1024x1024 AI 이미지와 640x800 원본 이미지가 동일한 387x489 슬롯에 꽉 차게 렌더링됨을 확인.
   - 2번 첨부 이미지에서 발생하던 상하 흰 공백 결함이 100% 해결됨.
4. **출력 프레임 포맷 및 색상 렌더링 검증**:
   - 4가지 색상(클래식 크림, 먹물 블랙, 단청 레드, 클래식 네이비)에 대해 웹 프레임(900x1350) 및 엽서 인쇄 프레임(1200x1800)을 직접 생성하여 확인 완료.
   - 1번 이미지와 100% 동일하게 스티커, 도장, 점선, 바코드, 영등포 브랜딩 텍스트가 구현되었으며 프레임 색상에 따라 텍스트/선의 명암 대비가 완벽하게 조정됨을 확인.

---

## 5. 조선 4컷 헤어스타일 및 다인원(커플/단체) AI 변환 프롬프트 전면 최적화

### 발생했던 문제점
1. **헤어스타일 어색함 (앞머리/옆머리 삐져나옴)**:
   - 왕(1컷)과 선비(2컷)의 관모(익선관, 갓) 착용 시, 사용자의 현대식 긴 앞머리나 옆머리가 관모 밖으로 어색하게 삐져나와 노비(머리띠/똥머리)에 비해 자연스러움이 떨어짐.
2. **다인원(2인 이상 커플/친구/가족) 변환 누락**:
   - 프롬프트가 단수(`the person`, `a Joseon-dynasty Korean king`) 위주로 기술되어, 2인 이상 촬영 시 전면의 남성만 왕/선비로 변환되고 동행한 여성은 현대 옷(검은색 민소매 나시, 목걸이 등)을 입은 채 그대로 방치되는 심각한 결함 발생.

### 개선 작업 내용
1. **다인원 개별 주체 인식 규칙 (`CRITICAL MULTI-PERSON TRANSFORMATION & INDEPENDENT SUBJECT RULE`) 전면 탑재**:
   - 프레임 안의 모든 인물을 독립된 주체(Primary Subject)로 인식하도록 명시.
   - `ABSOLUTELY NO PERSON SHOULD BE LEFT IN MODERN CLOTHING`: 단 한 사람도 현대 옷(민소매 나시, 티셔츠, 목걸이 등)을 입은 채 남지 않도록 강력한 네거티브 제약 부여.
2. **성별 및 역할에 따른 1:1 맞춤 복식/헤어 매핑**:
   - 👑 **1컷 (왕실 테마)**:
     - 남성: 곤룡포(용보 금박 흉배) + 익선관
     - 여성: 궁중 당의/활옷(비단 한복, 금박 장식) + 첩지/쪽진머리/비녀
   - 📚 **2컷 (선비/양반 테마)**:
     - 남성: 도포(백색/아이보리/연회색) + 흑립(갓)
     - 여성: 양반가 사대부 규수 최고급 실크 한복(우아한 저고리, 풍성한 치마) + 댕기머리/쪽진머리
   - 🎒 **3컷 (보부상 테마)**:
     - 남녀 공통 전통 장터 상인 무명 작업복 + 노란 패랭이 모자(양쪽 귀 목화솜 방울 2개) + 봇짐/지게
   - 🧹 **4컷 (노비/서민 테마)**:
     - 남녀 공통 거친 무명/삼베 옷(패치, 얼룩) + 이마 흰 머리띠 + 로우번(똥머리)
3. **헤어스타일 밀착 수납 가이드 (`AUTHENTIC HAIRSTYLE INTEGRATION`)**:
   - 익선관 및 갓 착용 시: 앞머리, 옆머리를 모두 뒤로 넘겨 망건(manggeon) 안으로 100% 수납하고 이마와 눈썹 라인을 말끔하게 드러내도록 규정.
4. **소스코드 및 프롬프트 문서 동기화 완료**:
   - `local-server/openai_transformer.py`의 `PROMPTS_JOSEON` 딕셔너리 최신화.
   - `c:\4cuts_pjt\조선시대 프롬프트\` 폴더 내 4개 원본 마크다운(`왕_프롬프트.md`, `선비_프롬프트.md`, `보부상_프롬프트.md`, `노비_프롬프트.md`) 전면 개정 완료.

---

## 14. 개선작업 14 (2026-09-13) - 다운로드 즉시 파기 프로세스 개편, 테두리 색상 동기화, 동영상 404 방지 및 Vercel 배포

- **문서 링크**: [`개선작업_14.md`](file:///c:/4cuts_pjt/개선작업_14.md)
1. **다운로드 즉시 파기 & 24시간 보존 2중 안전망 구축**:
   - 기존 24시간 일괄 삭제 대신, 고객이 모바일에서 사진/영상을 다운로드받는 즉시 구글 드라이브(`GAS Webhook action: delete_files`)와 로컬 서버(`/api/cleanup-session`)에서 영구 파기하도록 개편.
   - 미다운로드 시 24시간 동안 안전하게 유지 후 자동 파기되는 안심 안전망 유지.
2. **동영상 404 Not Found 에러 원천 해결**:
   - 사진 다운로드 시 세션 전체 파일이 삭제되어 아직 받지 않은 동영상까지 지워지던 결함을 `destroy_type`('photo' / 'video' / 'all') 필터로 정밀 분리하여, 사진 다운로드 시 동영상이 삭제되지 않도록 보존.
3. **인화 테두리 색상 모바일 및 QR 동기화**:
   - 태블릿에서 프레임 색상 변경 시 기본 파일(`ai_frame_{sid}.jpg`) 덮어쓰기, Google Drive 새 프레임 업로드 및 새 색상이 반영된 Dynamic QR 코드를 실시간 재생성하여 화면의 QR 이미지를 갱신.
4. **모바일 뷰어 상단 모서리 클리핑 최적화 및 일괄 버튼 제거**:
   - `.mobile-viewer-card`에 `overflow: hidden;` 및 상단 반경 보정을 적용하여 모서리 삐져나옴 완벽 해결.
   - 불필요한 `사진+영상 한 번에 저장` 버튼을 제거하고 고객 중심의 개별 탭 확인 저장 UX 복원.
5. **Vercel 실시간 배포 완료**:
   - GitHub `main` 브랜치 푸시(`691d553`)를 통해 실제 라이브 도메인 [https://4cut-pjt.vercel.app/download.html](https://4cut-pjt.vercel.app/download.html)에 최신 디자인 배포 완료.

---

## 15. 개선작업 15 (2026-09-13) - 스크롤 오류 해결, 다운로드 즉시 영구 파기 보장 및 동영상 스트리밍 최적화

- **문서 링크**: [`개선작업_15.md`](file:///c:/4cuts_pjt/개선작업_15.md)
1. **스크롤 전면 정상화**:
   - 키오스크 `style.css`에서 `html, body`의 `overflow: hidden` 제거 및 유연한 뷰포트 스크롤 복원 (765px 등 저해상도 화면에서도 하단 버튼/콘텐츠 완벽 노출).
   - `download.html`의 `.mobile-viewer-card`에서 `overflow: hidden`을 제거하여 모바일/PC 전 기기에서 부드러운 스크롤 복원.
2. **다운로드 즉시 영구 파기 완벽 보장 (Mixed Content 원천 제거)**:
   - 스마트폰 직결 QR 코드를 기본적으로 **로컬 Wi-Fi LAN IP(`http://<lan_ip>:8000/download.html?...`)** 로 생성하여 브라우저의 Mixed Content 차단을 원천 제거.
   - 세션 ID 추출 정규식을 `[a-zA-Z0-9_-]{6,16}`로 확장하여 `0bnr2y96` 등 모든 세션 ID 100% 인식 및 삭제 보장.
   - 기존 잔여 파일(`uploads/*0bnr2y96*` 20건) 전수 파기 완료.
3. **동영상 0.1초 고속 스트리밍 및 재생 최적화**:
   - 로컬 직결 라우팅을 통해 고속 대역폭으로 비디오 즉시 재생.
   - `onerror` 자동 복구 및 다이렉트 소스 다운로드 Fallback 탑재.
4. **Git Main 및 Vercel 실시간 배포 완료**:
   - GitHub 커밋 `20547a1` 푸시 및 Vercel 프로덕션 자동 배포 완료.



