# 🛠️ AI 인생 4컷 포토부스 개선 및 장애 해결 내역서 (Hotfix & History)

---

## 1. 개요 (Overview)
* **일시**: 2026년 8월 24일
* **목적**: 404 에러 해결 및 외부 태블릿/노트북 연동 중 발생한 (1) 촬영 이미지 미수집(엑박), (2) 구글 드라이브 비업로드, (3) 도메인/QR 주소 불일치 등 다각도 연동 오류의 장애 점검 및 핫픽스 개선 사항 기록.

---

## 2. 장애 원인 분석 & 조치 현황 (Troubleshooting)

### 2.1 [장애 1] 태블릿/노트북으로 촬영 진행 시 촬영본 확인 화면에서 이미지가 검은색/엑박으로 나타나는 현상
* **원인**: 
  - `startShootingSequence()`에서 4컷 연속 촬영을 수행할 때, canvas의 픽셀 데이터를 JPEG 파일로 인코딩하는 `canvas.toBlob()` 비동기 API가 실행 완료되기(await) 전에 다음 컷으로 즉시 루프가 진행되었습니다.
  - 이로 인해 미처 파일 변환이 완료되지 않은 상태에서 4컷 확인 화면(`screen-select`)으로 넘어가서 `capturedBlobs` 배열에 데이터가 채워지지 않아 엑박 현상이 발생했습니다.
* **조치**: 
  - `captureCanvasShot()`을 Promise를 반환하도록 감싸고 `toBlob` 완료 시 `resolve()` 하도록 구조 변경.
  - `startShootingSequence()` 루프 내부에 `await captureCanvasShot()`을 적용하여, 한 장 한 장의 사진 변환이 완벽히 인코딩된 후에 다음 촬영과 화면 전환이 일어나도록 동기화 조치.
  - 적용 파일: [`vercel-frontend/index.html`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/vercel-frontend/index.html) 및 [`local-server/templates/index.html`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/local-server/templates/index.html).

### 2.2 [장애 2] Vercel 사이트 및 모바일 QR 코드 스캔 시 도메인 불일치 및 404 에러
* **원인**:
  - `app.py`에 고정 배포 호스트가 `https://ai-4cut-studio.vercel.app`로 되어 있었으나, 실제 사용자의 배포 도메인은 `https://4cut-pjt.vercel.app` 이었기 때문에 QR 코드가 잘못 매핑되었습니다.
  - Vercel 배포 시 루트 폴더가 `vercel-frontend`가 아닌 `./` 기본 루트로 설정되어 `404 NOT FOUND`가 발생했습니다.
* **조치**:
  - `app.py` 내의 `base_host` 값을 실제 주소인 `https://4cut-pjt.vercel.app`으로 전면 수정하여 QR 코드가 정상 동작하도록 조치.
  - Vercel의 `Root Directory` 설정을 `vercel-frontend`로 변경하도록 매뉴얼 보강 및 배포 완료 확인.

### 2.3 [장애 3] 촬영 사진 및 비하인드 동영상이 구글 드라이브 폴더에 저장되지 않는 현상
* **원인**:
  - `local-server/` 디렉터리에 구글 서비스 계정 인증 파일인 **`google-key.json`** 이 존재하지 않아 로컬 Fallback 모드로만 구동 중이었습니다.
  - 구글 클라우드 콘솔에서 서비스 계정을 생성하더라도, 발급받은 서비스 계정 이메일(예: `xxxxx@yyyy.iam.gserviceaccount.com`)을 대상 구글 드라이브 사진/영상 폴더에 **'에디터(Editor)' 권한으로 공유(공동 작업자 추가)**하지 않으면 파일 생성 권한 오류가 발생합니다.
* **조치**:
  - API 결과 필드에 `"drive_upload_success"` 여부를 추가하여 백엔드 성공/실패 로깅을 사용자단까지 상세히 전달하도록 보완.
  - 서비스 계정 키 생성 및 구글 드라이브 공유 권한 설정을 매뉴얼에 명확히 명시.

---

## 3. 개선 작업 히스토리 목록 (Commit History)
1. **v2.0.1** (2026-08-24): `app.py`에 사용자 Vercel 호스트(`https://4cut-pjt.vercel.app`) 변경 및 `Request`를 통한 동적 처리 확장.
2. **v2.0.2** (2026-08-24): `vercel-frontend/index.html` 및 `local-server/templates/index.html`에서 canvas 비동기 Blob 변환 Promise 구조 도입(엑박 핫픽스).
3. **v2.0.3** (2026-08-24): `개선작업.md` 작성 및 전체 수정분 Git 푸시 완료.

---

## 4. 해결 가이드 (Action Item)

1. **`google-key.json` 복사**:
   구글 드라이브 API 권한을 발급받은 JSON 파일을 `local-server/` 디렉터리에 `google-key.json` 이름으로 저장합니다.
2. **구글 드라이브 폴더 공유 확인**:
   `google-key.json` 내부의 `client_email` 주소를 복사한 뒤, 구글 드라이브의 다음 두 폴더에 **'에디터(공동 작업자)'**로 공유 설정해 주세요.
   - [이용자 사진 저장소 폴더](https://drive.google.com/drive/folders/13KXZ_W7vurFPHbC_1tImac7ZLBlRuS3Q?usp=sharing)
   - [이용자 동영상 저장소 폴더](https://drive.google.com/drive/folders/1RgvKVq-J7JItVRD6M_9asnU8NfnaQ_dU?usp=sharing)
