# 🛠️ AI 인생 4컷 포토부스 개선 및 장애 해결 내역서 (Hotfix & History)

---

## 1. 개요 (Overview)
* **일시**: 2026년 8월 24일
* **프로젝트**: AI 4-CUT STUDIO (FastAPI + Stable Diffusion + Vercel Kiosk + Google Drive No-DB Storage)
* **목적**: 
  1. AI 이미지 변환 완료 후 프레임 미리보기가 엑박(Broken Image)으로 깨지는 현상 해결.
  2. ngrok 무료 도메인 브라우저 경고 페이지 정책 우회(Base64 인라인 데이터 즉시 렌더링).
  3. 구글 드라이브 사진/영상 스토리지 자동 업로드 연동을 위한 자격증명(`google-key.json`) 설정 및 권한 위임 완벽 가이드 제공.

---

## 2. 발생 장애 상세 분석 및 해결 내역 (Root Cause & Hotfix)

### 2.1 [장애 1] AI 변환 후 '변환 완료' 화면에서 AI 스타일 프레임 / 원본 사진 프레임이 엑박으로 표시되는 현상
* **원인**: 
  - 백엔드 AI 모델(Stable Diffusion) 변환 및 4컷 합성 자체는 성공적으로 실행되었으나, 클라이언트(Vercel) 화면의 `<img>` 태그가 백엔드의 이미지 URL (`https://...ngrok-free.dev/uploads/...`)을 GET 요청할 때 **ngrok 무료 티어의 보안 경고 페이지(HTML)**가 응답으로 반환되어 브라우저가 이미지를 렌더링하지 못함.
  - 일반 `fetch` 통신에는 `ngrok-skip-browser-warning` 헤더를 붙일 수 있지만, HTML의 `<img src="...">` 태그는 자체적으로 커스텀 헤더를 전송할 수 없음.
* **조치**: 
  - 백엔드(`app.py`)에서 4컷 합성 완료 즉시 고품질 JPEG 바이너리를 **Base64 Data URI**(`data:image/jpeg;base64,...`)로 인코딩하여 JSON 응답(`ai_frame_base64`, `orig_frame_base64`)에 포함하도록 개선.
  - 프론트엔드(`index.html`)가 별도의 추가 네트워크 요청 없이 응답받은 Base64 데이터를 `src`에 즉시 바인딩하도록 수정.
  - **결과**: ngrok 경고 페이지 간섭을 원천 차단하고 **0ms 지연으로 완성된 4컷 프레임이 즉시 화면에 렌더링**됨.

---

### 2.2 [장애 2] 촬영된 사진과 동영상이 구글 드라이브(사진/영상 저장소)에 업로드되지 않는 현상
* **원인**: 
  - 백엔드 서버 폴더([`local-server/`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/local-server))에 구글 클라우드 서비스 계정 키 파일인 **`google-key.json`** 이 누락되어 있어, 백엔드가 로컬 Fallback 스토리지 모드(`uploads/` 폴더 저장)로만 동작함.
  - 구글 드라이브 API 인증 Scope가 `drive.file`로 한정되어 있어 사용자가 생성한 외부 공유 폴더에 쓰기 권한이 제한될 수 있었음.
* **조치**: 
  - `app.py`의 Google Drive API 권한 Scope를 `https://www.googleapis.com/auth/drive` (전체 접근)으로 승격.
  - 구글 드라이브 업로드 상태를 JSON 응답(`drive_status_msg`, `drive_upload_success`)과 콘솔 로그에 명확히 표기.
  - 아래 **3장**에 `google-key.json` 발급 및 구글 드라이브 폴더 공유 설정 매뉴얼 수록.

---

## 3. 🚀 구글 드라이브 연동 완료를 위한 3분 필수 매뉴얼

구글 드라이브에 사진과 영상이 자동으로 업로드되도록 하려면 아래 **2단계 설정**이 필요합니다:

### [1단계] Google Cloud 콘솔에서 서비스 계정 키(`google-key.json`) 발급
1. [Google Cloud Console](https://console.cloud.google.com/) 접속 및 프로젝트 선택(또는 생성).
2. **API 및 서비스 > 라이브러리** 이동 ➔ **`Google Drive API`** 검색 후 **[사용]** 클릭.
3. **API 및 서비스 > 사용자 인증 정보** ➔ **[+ 사용자 인증 정보 만들기]** ➔ **[서비스 계정]** 선택.
4. 서비스 계정 이름(예: `4cuts-storage-bot`) 입력 후 생성 완료.
5. 생성된 서비스 계정 클릭 ➔ **[키(Keys)]** 탭 ➔ **[키 추가]** ➔ **[새 키 만들기(JSON)]** 클릭 ➔ JSON 파일이 PC로 다운로드됨.
6. 다운로드된 JSON 파일의 이름을 **`google-key.json`** 으로 변경하여 본 프로젝트의 [`local-server/`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/local-server) 폴더 바로 안에 넣어줍니다.

### [2단계] 구글 드라이브 대상 폴더에 서비스 계정 권한 부여
1. `google-key.json` 파일을 메모장으로 열고 **`client_email`** 항목에 적힌 이메일 주소(예: `4cuts-storage-bot@xxx.iam.gserviceaccount.com`)를 복사합니다.
2. 아래 두 구글 드라이브 폴더 링크에 접속합니다:
   - 📸 [이용자 사진 저장소 폴더 열기](https://drive.google.com/drive/folders/13KXZ_W7vurFPHbC_1tImac7ZLBlRuS3Q?usp=sharing)
   - 🎥 [이용자 동영상 저장소 폴더 열기](https://drive.google.com/drive/folders/1RgvKVq-J7JItVRD6M_9asnU8NfnaQ_dU?usp=sharing)
3. 각 폴더 이름 우측의 `▼` 클릭 ➔ **[공유]** 클릭 ➔ 복사한 **`client_email` 주소를 붙여넣고 권한을 '편집자(에디터)'**로 지정 후 [보내기/공유] 완료.

---

## 4. 변경 이력 (Version History)
* **v2.0.1** (2026-08-24): Vercel 도메인(`https://4cut-pjt.vercel.app`) 및 QR 코드 링크 연동 보정.
* **v2.0.2** (2026-08-24): 카메라 웹캠 캡처 비동기 `Promise/await` 동기화 핫픽스.
* **v2.0.3** (2026-08-24): `app.py` Base64 프레임 인라인 반환 도입 (ngrok 엑박 원천 해결) 및 Drive Scope 승격.
