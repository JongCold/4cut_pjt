# AI 4컷 포토부스 개선 작업 보고서 (개선작업_2.md)

## 1. 개요
본 보고서는 이미지 변환 시 발생하던 **이중 얼굴 겹침(고스트) 현상 제거**, 모바일 다운로드 페이지의 **수직 스크롤 차단(Scroll Lock) 해제**, 그리고 **Google Apps Script(GAS) 403 리디렉션 처리 보강**에 대한 원인 분석 및 최종 개선 내역을 정리합니다.

---

## 2. 주요 개선 및 문제 해결 내역

### 2.1. 인물 이미지 잔상 및 이중 얼굴(여성 얼굴 겹침) 현상 완벽 제거
- **문제 현상:** 첨부 이미지 1번과 같이 AI 변환 결과물 우측 상단 컷에서 사용자 얼굴 위에 여자 아이의 얼굴 특징 및 눈동자 잔상이 겹쳐져 보이는 현상 발생.
- **원인 분석:** 
  - 기존 파이프라인에서 원본 사진과 AI 변환 이미지를 전체 캔버스 단위로 `Image.blend()` 방식으로 알맹이 합성을 수행했습니다.
  - 이 과정에서 AI 모델이 생성한 얼굴 위치와 원본 얼굴 위치가 수 픽셀 차이나면서, 원본 이목구비와 AI 이목구비 두 개가 반투명하게 겹쳐 보이는 고스트(Ghosting) 현상이 일어났습니다.
- **조치 및 개선 사항:**
  1. **전체 캔버스 알파 블렌딩 제거:** 이중 겹침을 유발하던 `Image.blend()`를 완전 제거하고, AI 변환 파이프라인의 결과물을 깨끗한 단일 렌더링 이미지로 직접 사용하도록 변경했습니다.
  2. **Img2Img Strength 수치 정밀 제어 (`0.30 ~ 0.32`):** 원본 얼굴 위치와 구도가 100% 동일하게 유지되면서 스타일만 고품질로 변환되도록 strength 수치를 조정했습니다.
  3. **강력한 억제 네거티브 프롬프트 적용:** 모든 필터에 `multiple faces, double face, extra face, ghosting, overlapping faces, female face overlay`를 필수 등록하여 2인 이상의 얼굴이나 여성 이미지 레이어 겹침을 완전 차단했습니다.

---

### 2.2. 모바일 다운로드 뷰어(`download.html`) 수직 스크롤 락(Lock) 해제
- **문제 현상:**
  - 첨부 이미지 1번: 하단 개인정보보호 방침 박스를 읽기 위해 터치 스크롤을 내려도 화면이 움직이지 않던 문제.
  - 첨부 이미지 2번: '사진과 영상 모두 한 번에 보기' 버튼을 눌렀을 때, 아래쪽 비하인드 동영상 카드로 스크롤이 내려가지 않던 문제.
- **원인 분석:**
  - `download.html`이 불러오는 공통 `style.css` 내부에 `body { overflow: hidden; height: 100vh; }` 키오스크 전용 스크롤 잠금 옵션이 작성되어 있어, 모바일 브라우저의 수직 스크롤 조작이 전면 차단되어 있었습니다.
- **조치 및 개선 사항:**
  1. **스크롤 잠금 완전 해제:** `download.html`에 `html, body { overflow-y: auto !important; height: auto !important; touch-action: pan-y !important; }` 스타일을 선언하여 모바일 스마트폰에서 자유롭게 위아래로 터치 스크롤할 수 있도록 보장했습니다.
  2. **자동 스크롤 이동 연동:** '사진과 영상 모두 한 번에 펼쳐보기' 버튼 터치 시, 아래쪽 비하인드 동영상 영역으로 화면이 자연스럽게 부드럽게 스크롤(Smooth Scroll)되도록 JavaScript 동작을 구현했습니다.

---

### 2.3. Google Apps Script(GAS) Webhook 403 리디렉션 처리 보강
- **원인 분석:**
  - 기존 `google_drive_gas.js` 스크립트에 POST 요청 처리인 `doPost(e)`만 정의되어 있고, GET 요청 및 헬스체크용 `doGet(e)`가 누락되어 있었습니다.
  - Python `requests` 모듈이 HTTP 302 리디렉션을 추적하는 과정에서 `script.googleusercontent.com`으로 GET 전환 시 `doGet`이 없어 403/405 에러 응답을 반환했습니다.
- **조치 사항:**
  - `local-server/google_drive_gas.js` 스크립트에 `doGet(e)` 헬스체크 JSON 응답 함수를 추가하고, 지정 폴더 ID 접근 실패 시 루트 폴더로 자동 전환되는 안전 예외 처리 구성을 완료했습니다.

---

## 3. 최종 시스템 검증 및 변경 파일 요약

| 구분 | 파일 경로 | 변경 내용 |
| :--- | :--- | :--- |
| **AI 파이프라인** | `local-server/concept_transformer.py` | 이중 얼굴 겹침(고스트) 제거, 정밀 strength & 네거티브 프롬프트 적용 |
| **모바일 뷰어** | `vercel-frontend/download.html` | `overflow-y: auto !important` 적용으로 수직 스크롤 100% 복구 및 동영상 자동 스크롤 구현 |
| **구글 스크립트** | `local-server/google_drive_gas.js` | `doGet(e)` 헬스체크 및 예외 처리 구성을 포함한 v2.0 스크립트 개편 |
| **개선 작업서** | `개선작업_2.md` | 현 2차 개선 내역 보고서 작성 |

---
**작성일:** 2026-08-25  
**작성자:** Antigravity AI Assistant
