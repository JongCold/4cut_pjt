# AI 4컷 포토부스 구글 드라이브 연동 및 자동화 가이드

본 문서는 사진 및 동영상의 구글 드라이브 자동 저장과 24시간 자동 파기 시스템을 온전히 연동하기 위한 설정 매뉴얼입니다.

---

## 1. 구글 드라이브 API 발급 및 서비스 계정 키 생성 방법
구글 서비스 계정(Service Account)을 활용한 로컬 서버 다이렉트 통신(API) 및 24시간 파기 백엔드 가동을 위한 사전 준비 과정입니다.

1. **Google Cloud Console 접속:**
   - [Google Cloud Console](https://console.cloud.google.com/)에 로그인합니다.
2. **프로젝트 생성:**
   - 상단 프로젝트 선택 드롭다운 클릭 후 `[새 프로젝트]`를 생성합니다.
3. **API 활성화:**
   - 왼쪽 메뉴 `[API 및 서비스] -> [라이브러리]` 클릭.
   - 검색창에 `Google Drive API` 검색 후 `[사용]` 버튼을 누릅니다.
4. **서비스 계정 생성:**
   - `[API 및 서비스] -> [사용자 인증 정보]` 클릭.
   - 상단 `[+ 사용자 인증 정보 만들기] -> [서비스 계정]` 선택.
   - 서비스 계정 이름을 입력하고 `[만들기 및 계속하기]` 클릭 후 `[완료]`를 누릅니다.
5. **키(JSON) 발급:**
   - 생성된 서비스 계정 이메일을 클릭하고 상단 `[키]` 탭으로 이동합니다.
   - `[키 추가] -> [새 키 만들기] -> [JSON]` 형식을 선택하고 `[만들기]`를 클릭하면 다운로드됩니다.

---

## 2. 현재 소스 코드에서 구글 드라이브 키 적용 방법
로컬 백엔드 서버가 구글 드라이브 API를 사용할 수 있도록 키 파일을 배치합니다.

1. **인증 파일 이름 변경:**
   - 다운로드한 JSON 키 파일의 이름을 **`google-key.json`**으로 변경합니다.
2. **로컬 프로젝트 폴더에 배치:**
   - 변경한 `google-key.json` 파일을 포토부스 백엔드 폴더 내부인 **`local-server/`** 경로 밑에 넣어줍니다.
   - *올바른 배치 상대 경로:* `local-server/google-key.json`
3. **구글 드라이브 폴더 공유 (필수):**
   - 구글 드라이브에 접속하여 사진 전용 폴더와 영상 전용 폴더를 각각 생성합니다.
   - 각 폴더의 **[공유]** 설정을 누르고, `google-key.json` 내부에 명시된 서비스 계정 이메일(예: `4cuts-storage-bot@[PROJECT_ID].iam.gserviceaccount.com`)을 복사하여 **편집자(Editor)** 권한으로 추가/공유해 줍니다.
   - 각 폴더의 브라우저 주소창 URL 끝부분에 나오는 폴더 ID를 확인하여 `app.py`의 `GOOGLE_PHOTO_FOLDER_ID`와 `GOOGLE_VIDEO_FOLDER_ID` 값에 등록합니다.

---

## 3. Google Apps Script(GAS) 콘솔 사용 방법
서비스 계정의 0 Byte 용량 한계 정책을 우회하여 사용자의 개인 5TB 용량 드라이브에 파일을 직접 업로드하기 위해 GAS 콘솔을 사용합니다.

1. **GAS 홈페이지 접속:**
   - [Google Apps Script](https://script.google.com/)에 본인 구글 계정(`[USER_ACCOUNT]@gmail.com`)으로 로그인합니다.
2. **콘솔 대시보드:**
   - 왼쪽 상단의 **`[새 프로젝트]`** 버튼을 클릭하여 새 스크립트 작성 창을 실행합니다.
   - 프로젝트 제목(예: `인생4컷 저장소`)을 더블클릭하여 이름을 지정합니다.

---

## 4. GAS 소스코드 반영 및 수정 방법
제공된 GAS 웹훅 최신 스크립트(v2.2)를 반영합니다.

1. **코드 붙여넣기:**
   - 프로젝트 폴더의 [`local-server/google_drive_gas.js`](file:///c:/Users/USER/Downloads/4cuts_pjt-20260824T051904Z-1-001/4cuts_pjt/local-server/google_drive_gas.js) 내용을 전부 복사합니다.
   - GAS 콘솔 편집기의 기존 `Code.gs` 내용을 전체 삭제하고 붙여넣습니다.
2. **프로젝트 저장:**
   - 코드 붙여넣기 후 상단 툴바의 **`[프로젝트 저장 💾]`** 아이콘을 클릭하거나 단축키 **`Ctrl + S`**를 눌러 프로젝트를 저장합니다.
3. **최초 실행 권한 승인 (가장 중요):**
   - 툴바의 실행 함수 선택 드롭다운에서 **`doPost`**를 선택한 후 바로 왼쪽의 **`[▶ 실행]`** 버튼을 누릅니다.
   - 구글 계정 보안 접근 경고 팝업이 뜨면 **`[권한 검토]`** 클릭 ➔ 본인 구글 계정 선택.
   - 하단 **`[고급]`** 클릭 ➔ **`[인생4컷(안전하지 않음)으로 이동]`** 클릭 ➔ **`[허용]`** 클릭.
   - 실행 로그에 완료 메시지가 확인되면 권한 연동이 완료된 것입니다.

---

## 5. GAS 웹 앱 배포 방법
수정된 코드를 백엔드 서버가 호출할 수 있는 웹 주소(API Webhook URL)로 배포합니다.

1. **배포 시작:**
   - 편집기 우측 상단 블루 버튼 **`[배포] -> [새 배포]`**를 클릭합니다.
2. **유형 구성:**
   - 톱니바퀴 모양 아이콘을 누르고 유형으로 **`[웹 앱]`**을 선택합니다.
3. **배포 매개변수 설정:**
   - **설명:** `인생4컷 저장소 배포 v2.2`
   - **다음 사용자 권한으로 실행:** **`나 ([USER_ACCOUNT]@gmail.com)`** 선택.
   - **액세스 권한이 있는 사용자:** **`모든 사용자(Anyone)`** 선택.
4. **웹 앱 URL 획득:**
   - 하단 **`[배포]`** 클릭 시 제공되는 **웹 앱 URL (https://script.google.com/macros/s/.../exec)**을 복사합니다.
   - 복사한 URL을 백엔드 서버 `local-server/app.py` 파일 36번 라인의 `GAS_WEBHOOK_URL` 변수값으로 입력해 줍니다.

---

## 6. 트리거(자동 삭제 스케줄러) 적용 방법
로컬 부스 PC가 종료된 상황에서도 구글 클라우드가 알아서 24시간이 지난 구글 드라이브 파일들을 영구 파기하도록 자동 트리거를 설정합니다.

1. **트리거 메뉴 이동:**
   - GAS 편집기 화면 왼쪽 메뉴바에서 시계 모양 아이콘 **`[트리거 ⏰]`** 메뉴를 클릭합니다.
2. **트리거 생성:**
   - 오른쪽 아래 파란색 **`[+ 트리거 추가]`** 버튼을 클릭합니다.
3. **상세 타이머 옵션 구성:**
   - **실행할 함수 선택:** **`cleanupOldFiles`** 선택 (저장 후 실행 목록에 뜹니다).
   - **실행할 배포 선택:** `Head`
   - **이벤트 소스 선택:** **`시간 기반`**
   - **트리거 기반 시간 유형 선택:** **`시간 단위 타이머`** (또는 분 단위 타이머)
   - **시간 간격 선택:** **`매시간`** (또는 15분마다)
4. **저장 및 연동 완료:**
   - **`[저장]`**을 클릭하면 백업 완료되며, 매시간 백업 스토리지 청소가 클라우드 내부에서 영구히 자동으로 실행됩니다.

---

## 7. 주요 에러 원인 및 해결 FAQ (트러블슈팅)

### Q1. `[Auto Cleanup Error] 구글 드라이브 파일 삭제 실패: 403 insufficientFilePermissions` 에러가 왜 발생하나요?
* **원인**: 구글 드라이브에 업로드된 사진/동영상은 GAS를 통해 **사용자 개인 구글 계정의 소유권**으로 생성됩니다. 구글 드라이브 정책상 편집자 권한의 서비스 계정은 다른 사용자가 소유한 파일을 영구 삭제(`delete`)할 수 없습니다.
* **해결 및 정상 동작**: 파일의 영구 파기는 사용자 계정 권한으로 실행되는 **GAS 트리거(`cleanupOldFiles`)가 전담**하여 매시간 정상 삭제합니다. 로컬 백엔드는 이 에러를 자동으로 인지하고 안전하게 위임 처리합니다.

### Q2. `[Google Drive API] ⚠️ 업로드 실패: storageQuotaExceeded (Service Accounts do not have storage quota)` 에러가 왜 발생하나요?
* **원인**: GAS Webhook 호출이 실패하여 2차 Fallback인 서비스 계정 직접 업로드가 시도되었으나, 구글의 정책상 서비스 계정은 자체 저장 공간이 0MB(무할당)이므로 차단되는 현상입니다.
* **해결**:
  1. GAS 콘솔에서 최신 코드를 반영한 뒤 **`[새 배포]`**를 진행하여 `액세스 권한: 모든 사용자(Anyone)`로 설정된 최신 Webhook URL을 `app.py`의 `GAS_WEBHOOK_URL`에 등록합니다.
  2. GAS가 정상 배포되면 사용자의 **개인 5TB 저장공간**으로 업로드되어 이 에러가 발생하지 않습니다.
  3. 설령 드라이브 업로드가 지연되더라도, 파일은 **로컬 `uploads/` 폴더에 완벽히 보관**되므로 포토부스 화면 렌더링 및 모바일 1-Click QR 다운로드는 정상 작동합니다.

---
*본 가이드는 `JongCold/4cut_pjt` 3차~6차 유지보수를 기반으로 검증 및 제작되었습니다.*
