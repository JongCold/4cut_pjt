# 개선 작업 17: 기본 테마 (자연스러운 한국형 인생4컷 원본 보존 & 미세 색감 보정) 추가

## 1. 작업 배경 및 목적
사용자의 요청에 따라 4컷 포토부스에 **[기본 테마 (BASIC / NATURAL PHOTO BOOTH)]**를 신규 추가하였습니다.
촬영된 인물의 외모, 포즈, 표정, 의상, 배경 등 진본성을 100% 보존하면서 인위적인 왜곡이나 만화화 없이 한국형 전문 포토부스 인화물 수준의 미세한 노출/화이트밸런스/피부결 톤 보정만을 수행하도록 파이프라인을 구축하였습니다.

---

## 2. 주요 변경 내역

### 1) OpenAI 변환 파이프라인 확장 (`local-server/openai_transformer.py`)
- **`BASIC_CORE_BASE` 프롬프트 구축**:
  - 원본 사진 절대 기준 보존 (No redesign, no stylize, no face reconstruction, no identity change).
  - 다인원 인원수, 이목구비, 안경, 의상, 포즈, 표정 100% 보존.
  - 극사실적 질감 유지 및 엄격한 네거티브 제약(No illustration, no cartoon, no beauty-filter face 등).
  - 미세한 화이트밸런스, 노출, 명암비, 포토부스 인화 색감 보정만 허용.
- **`PROMPTS_BASIC` 딕셔너리 구축 (컷 0~3)**:
  - 1컷: 맑은 화이트밸런스 및 노출 교정
  - 2컷: 생기 있는 피부톤 및 포토부스 일관 조명 유지
  - 3컷: 소프트 하이라이트 및 자연스러운 암부 디테일 유지
  - 4컷: 4컷 포토스트립 인화 톤 완성
- **`transform_single_image_openai`**:
  - `style == "basic" or sub_theme == "basic"` 분기 추가.

### 2) 백엔드 라우팅 및 진행 상태 등록 (`local-server/app.py`)
- `/api/transform_single`: `is_basic` 단계별 실시간 진행 상태 메시지("자연스러운 인생4컷 1~4컷 노출/톤 보정 중...") 추가.
- `/api/transform`: `is_basic` 시 포토 프레임 브랜드 타이틀 `기본 4컷 (NATURAL BASIC)` 매핑.

### 3) 프론트엔드 키오스크 UI 확장 (`local-server/templates/index.html`)
- **홈 화면 안내 카드**: 1단계 컨셉 선택 설명에 `기본 4컷(내추럴)` 포함.
- **카테고리 탭**: 4번째 탭 `<button id="tab-cat-3">기본 4컷</button>` 추가.
- **캐러셀 뷰포트**: 4번째 슬라이드(Slide 3: `기본 4컷 (내추럴 보정)`) 및 4번째 인디케이터 닷(`dot-3`) 추가.
- **자바스크립트 `conceptList`**: `{ style: 'basic', sub: 'basic', name: '기본 4컷 (내추럴 원본)', badge: '🌿 기본 4컷' }` 등록.
- **촬영 부스 연동**: 기본 테마 선택 시 1~4컷 이모지(`📷`, `✨`, `🎞️`, `💖`) 및 가이드 텍스트 자동 동기화.

### 4) 프롬프트 명세서 문서화
- [`c:\4cuts_pjt\기본4컷 프롬프트\기본이미지 생성 프롬프트.md`](file:///c:/4cuts_pjt/기본4컷%20프롬프트/기본이미지%20생성%20프롬프트.md) 생성 완료.

---

## 3. 검증 결과
1. **Python 컴파일 및 임포트 검증**: `openai_transformer.py` 및 `app.py` 구문 검사 및 단위 테스트 통과.
2. **브라우저 실제 UI 검증**:
   - 상단 4개 탭('인생 4컷', '조선 4컷', '픽셀 4컷', '기본 4컷') 정상 렌더링.
   - '기본 4컷' 탭 클릭 시 슬라이드 3 이동 및 하단 선택 텍스트 '기본 4컷 (내추럴 원본)' 정상 표시.
   - 촬영 부스 진입 시 4개 컷 이모지(`📷`, `✨`, `🎞️`, `💖`) 정상 배치 확인 완료.
