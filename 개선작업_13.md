# 개선작업 13: 모바일/태블릿 변환 완료 화면 세로 스크롤 활성화 및 탭·버튼 잘림 해결

**문서 번호**: 4CUTS-IMPROVE-013  
**작업 일시**: 2026-09-10  
**작업 요약**:
1. 모바일/스마트폰 및 태블릿 브라우저에서 `[화면 5: 변환 완료 (screen-result)]` 진입 시 위아래 스크롤이 되지 않던 결함 원천 해결.
2. 상단 고정 헤더(70px) 뒤로 파묻히던 상단 탭 바(`✨ AI 스타일 4컷`, `📷 원본 촬영 4컷`) 노출 정상화.
3. 화면 하단에서 잘리던 액션 버튼(`선택 완료 (QR & 인화) ➔`) 및 하단 내비게이션 바 안전 영역(`env(safe-area-inset-bottom)`) 확보.
4. 모바일 주소창 동적 높이 변화 대응을 위한 `100dvh`(Dynamic Viewport Height) 전면 적용.

---

## 1. 문제 분석 및 원인 규명

| 문제 현상 | 근본 원인 | 해결 조치 |
| :--- | :--- | :--- |
| **모바일에서 위아래 터치 스크롤 불가** | `.result-large-content`에 `overflow: hidden !important;`가 하드코딩되어 있어, 화면 높이가 작은 스마트폰에서 내용이 화면 밖으로 넘쳐도 스크롤 이벤트가 완전히 차단됨. | `overflow-y: auto !important;` 및 `-webkit-overflow-scrolling: touch;` 적용으로 부드러운 모바일 관성 스크롤 복원. |
| **상단 탭 버튼 상단부가 잘림** | 상단 고정 헤더(`.fixed-header`, height: 70px)가 위치함에도 `.result-large-content`의 패딩이 `padding-top: 6px;`로 덮어써져 상단 70px 영역(탭 바)이 헤더 뒤로 파묻힘. | `padding-top: 78px !important;`로 헤더 아래 여유 공간을 확보하여 탭 바가 한눈에 시원하게 보이도록 수정. |
| **하단 '선택 완료' 버튼이 잘려 터치 불가** | 1. 고정 푸터 뒤 여백 부족 (`padding-bottom: 100px` 미달).<br>2. 모바일 브라우저 주소창/네비바로 인해 `height: 100vh`가 실제 화면보다 커져 하단 푸터가 화면 밖으로 밀려남. | 1. `padding-bottom: 110px !important;` 및 하단 상태 텍스트 마진 확보.<br>2. `min-height: 100dvh; height: 100dvh;` 적용 및 푸터에 `padding-bottom: max(16px, env(safe-area-inset-bottom))` 적용. |
| **대형 프레임 크기 경직성** | `.four-cut-frame-large img`의 고정 flex 높이(`calc(100vh - 220px)`)로 인해 작은 화면에서 이미지가 부모를 뚫고 나가거나 잘림. | `max-height: 500px;` (세로 700px 이하 기기에서는 `max-height: 380px;`), `width: auto; max-width: 90%;`로 반응형 최적화. |

---

## 2. 수정 파일 내역

1. **`local-server/templates/style.css`**: 로컬 키오스크 및 태블릿/스마트폰 접속 시 즉시 반영
2. **`vercel-frontend/style.css`**: Vercel 클라우드 프론트엔드 동기화 반영
3. **`style.css`**: 루트 스타일시트 최신 동기화

---

## 3. 핵심 CSS 수정 코드

```css
/* 모바일 브라우저 동적 뷰포트(dvh) 대응 */
body {
  min-height: 100vh;
  min-height: 100dvh;
  overflow: hidden;
}

.app-container {
  height: 100vh;
  height: 100dvh;
}

/* 하단 고정 푸터 안전 영역 확보 */
.fixed-footer {
  padding: 16px 20px;
  padding-bottom: max(16px, env(safe-area-inset-bottom));
  background-color: rgba(255, 255, 255, 0.92);
  backdrop-filter: blur(12px);
  z-index: 100;
}

/* 변환 완료 상세 뷰 모바일 세로 스크롤 복원 */
.result-large-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 100%;
  padding-top: 78px !important;  /* 상단 헤더(70px) 가림 방지 */
  padding-bottom: 110px !important; /* 하단 푸터 버튼 가림 방지 */
  padding-left: 14px;
  padding-right: 14px;
  overflow-y: auto !important;  /* 모바일/태블릿 세로 스크롤 전면 허용 */
  -webkit-overflow-scrolling: touch; /* 부드러운 터치 관성 스크롤 */
  box-sizing: border-box;
  scroll-behavior: smooth;
}

/* 슬림 퍼플 스크롤바 */
.result-large-content::-webkit-scrollbar {
  width: 6px;
}
.result-large-content::-webkit-scrollbar-thumb {
  background: rgba(139, 92, 246, 0.25);
  border-radius: 10px;
}

/* 대형 4컷 프레임 반응형 */
.four-cut-frame-large img {
  height: auto;
  max-height: 500px;
  width: auto;
  max-width: 90%;
  object-fit: contain;
  border-radius: 8px;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
  background-color: #ffffff;
}

@media (max-height: 700px) {
  .four-cut-frame-large img {
    max-height: 380px;
  }
}
```
